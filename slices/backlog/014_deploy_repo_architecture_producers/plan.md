# Slice 014 — KubeCoderDeploy and ArgoCDDeploy each publish their own architecture, generated by the `aac-tools` image, so no app drops out of the federated model when it moves to Argo CD

## Requirements / rulings

Requirements are the operator's words, from `slice.md`'s "Re-cut on 2026-09-20" and "Carried in
from the 2026-09-20 design session" sections, which win over the 2026-08-15 record between them.
Rulings are dated; a ruling that corrects an earlier one replaces it in place.

- **R1 — The model loses nothing through KubeCoder's cutover.** *"The architecture file is a first
  class element the current HelmCharts setup. Actually, it's two parts: the static files and the
  generator. Both absolutely need to keep working as we switch over."* — *"This one is important.
  It reads like we have some optional thing that may break. That's not how I want to run this
  project."*

- **R2 — KubeCoderDeploy's producer publishes prd only, from the `prd` branch.** *"Do we push
  architecture for dev? Implicitly, yes, today, but I have no need for it in my architecture
  manifest. Can't we just not publish dev architecture and only deploy architecture for
  kubecoder-prd?"* — *"If (and we should) use the prd branch, that means we're publishing the right
  thing."* — *"Don't make it a generator rule. I may have different needs for other apps."* Why one
  stage per pipeline: *"The artifact is attached to the pipeline. If the pipeline listens to dev and
  prd, the architecture would flap."* The guard is the generator's required, single-valued
  `--stage` and nothing else — no branch check (*"It knows the stage, right? It has to build it to
  build the namespace. Can't we use that?"*). The static file `charts/kubecoder/architecture.yaml`
  moves into KubeCoderDeploy and gains an explicit `introduced:`.

- **R3 — Element UUIDs are kept.** *"I agree on the rest."* — against the recommendation to keep the
  uuid5 namespace constant and natural keys so a moved element keeps its id. Acceptance: the new
  producer's artifact equals the KubeCoder prd subset `helm-charts` publishes today — same ids, only
  the producer differs.

- **R4 — HelmCharts does not move onto the image.** *"No. I must assume that it can keep working as
  is. Honestly, I'd prefer you patch it if you need changes in it. I want to limit the amount of
  work we do on that repo."* What HelmCharts owes is one test pinning that a stage flipped to
  `reconciler: argo-cd` leaves the artifact without failing the build.

- **R5 — Every deploy repo gains a `Jenkinsfile.architecture`; both deploy repos plus the reusable
  pattern.** The operator's ask: *"The new deploy repos all need to gain a Jenkinsfile.architecture
  pipeline delivering the same artifact scoped to that repo"*; scope: *"Both, plus the reusable
  pattern"* — KubeCoderDeploy **and** ArgoCDDeploy gain producers (ArgoCDDeploy's folded in from
  slice 009's close-out S8: the model shows an internet-facing webhook relay pointing at nothing
  and no model of Argo CD at all), and the slice produces the how-to so every future migrated app
  carries its own. The `AaC/<Repo>` Jenkins jobs and the `pipeline-producers.yaml` PR are
  *"Owed to the operator"*; registration follows the producer's first green build.

- **R6 — A `containerTemplates` entry for `aac-tools` in JenkinsPipelineUtils**, referencing the
  image by a floating tag — *"Yes on the floating tag."*

- **R7 — The equality fixture stops being a second copy** (slice 024 close-out S4, operator: *"Fold
  into 014 please."*). `aac-tools/checks/kubecoder-architecture.yaml` is deleted once KubeCoderDeploy
  commits its own `architecture.yaml`, and the handover check proves equality for the file the
  pipeline actually renders — the clone's own committed judgment layer, not a copy laid over it.

- **Ruling (2026-09-21) — Argo CD's model carries both of the webhook relay's edges; the generator
  change landed before the run.** Operator: *"Agree"* (refinement D1). The generator's per-image
  `upstream` annotation took exactly one wire; it now also takes a list, each wire resolved on its
  own (ArgoCDTools `ff7e443`, made in the planning session, outside this slice). ArgoCDDeploy's
  judgment layer declares both wires — webhook-relay served by Argo CD's server and by its
  ApplicationSet controller.

- **Ruling (2026-09-21) — at a handover, the new producer is registered before the app's stage is
  flipped.** Operator: *"Agree"* (refinement D2). Register first, then flip: while both producers
  declare the kept ids the collector fails on the duplicates and publishes nothing, so the model
  keeps the app as it was; the flip's HelmCharts architecture build clears the duplicates and the
  next collector run is green with the new producer owning the same ids. The architecture job is
  red for the minutes between the two steps — expected. The precondition stands either way: the new
  producer's first green build comes before its registration. The how-to states this order; slice
  012's cutover follows it.

- **Settled by the session, agreed on reading (2026-09-21).** Argo CD's producer is **generated**,
  the same shape as KubeCoder's, not hand-authored, and owns the Argo CD product element nobody
  publishes today. Producer ids are **`kubecoder-deploy`** and **`argocd-deploy`**. KubeCoder's
  annotation file is copied into KubeCoderDeploy now; HelmCharts' copy stays until slice 012 deletes
  `charts/kubecoder/`, because HelmCharts publishes KubeCoder until the prd flip. The how-to is a
  section of the Argo CD runbook (`/work/Ansible/docs/runbooks/argocd.md`) beside registering an
  app — not a separate document. Each deploy repo's local test verb generates and validates its
  artifact through the `aac-tools` toolchain. ArgoCDDeploy's producer, which builds `main`, is the
  one created and registered first, so the new pipeline shape is proven outside KubeCoder's
  cutover. The run pushes every repo it touches except the two held below; the shared Jenkins
  library has no local gate and every pipeline loads it, so its new entry copies an existing
  entry's shape and the operator's first `AaC/ArgoCDDeploy` build is its canary (the trade slice 011
  ruled in for the same library).

#### Grounding — premise corrections

Established by the planning session on 2026-09-21 against HelmCharts `78498dc`, ArgoCDTools
`ff7e443`, KubeCoderDeploy `092a444`, ArgoCDDeploy `3246e89`, JenkinsPipelineUtils `2c43b06` and
`/work/Architecture` `898df78`. `slice.md`'s own readings are superseded where they differ.

- **G1. The three stale `producer-manual.md` citations no longer exist.** `grep producer-manual` is
  empty over `/work/Ansible/CLAUDE.md`, `/work/DockerImages/CLAUDE.md` and
  `/work/HelmCharts/CLAUDE.md`. The open question drops out; nothing to do.
- **G2. KubeCoderDeploy has no `prd` branch** — only `main`, locally and on `origin`. Slice 012
  creates `prd` at the prd cutover. So `AaC/KubeCoderDeploy` can have its first build only then;
  this slice commits the producer to `main`, which `prd` is born from. No phase can witness a
  Jenkins build — the jobs are the operator's.
- **G3. HelmCharts already drops a flipped stage, silently, and nothing pins it.** HelmCharts'
  `tools/chart_tools/gen_architecture.py` `releases()` (`:209`) enumerates every
  `configs/prd/<chart>/<stage>/` and asks `deploy config` for its metadata;
  `tools/deploy/deploy_cli/release.py:186` returns `chart_name = cfg.get("chart", chart_dir) if ours
  else None` with `ours` = reconciler is `jenkins`; `gen_architecture.py:587` skips a release with
  no `chart_name`. The one live instance, `configs/prd/argocd/prd/release.yaml`
  (`reconciler: argo-cd`), is absent from the live dataset — zero `argo` matches from any producer.
  `tests/test_gen_architecture.py` never mentions `chart_name` or `reconciler`. So R4 is a test
  only; no patch. (Slice 024's planning note G14, that the published artifact still claimed Argo
  CD's deployment, was wrong.) `configs/prd/kubecoder/` still has no `release.yaml`.

#### Grounding — verified facts the plan rests on

- **G4. The deploy-repo generator's contract.** `/work/ArgoCDTools/aac-tools/image/gen_architecture.py`
  reads the judgment layer `architecture.yaml` at the deploy repo root (`annotations()`,
  `:283-306`) and **requires** `introduced:` — no git fallback. Keys: `images`, `cnpg`, `products`
  (owned product elements, checked before the published dataset), `exposures`, `webUi`,
  `mcpClients` (module docstring, `:41-80`). It writes `docs/architecture/<producer>.yaml`, a build
  output — not committed. It keys every id on `chart/Chart.yaml`'s `name`, while Argo keys the app
  on the registry directory (ANS-85, open): both deploy repos agree today (`kubecoder`, `argocd`),
  and the how-to must say the two have to match.
- **G5. KubeCoder's judgment layer.** `/work/HelmCharts/charts/kubecoder/architecture.yaml` holds an
  `images:` block of five entries. `/work/ArgoCDTools/aac-tools/checks/kubecoder-architecture.yaml`
  is that block verbatim plus an explanatory header and `introduced: '2026-06-17'`, the date
  HelmCharts derives from git today. `aac-tools/checks/handover_equality.py` points at the fixture
  (`FIXTURE`, `:41`), takes `--annotations` (`:221-223`) and copies it over the clone's root before
  rendering (`:259`). The check ran green against the live dataset at slice 024: 9 elements, 16
  relations, id for id, with one expected `gap:` line for `kube-coder-tunnel-reclaim`. HelmCharts
  draws four cross-stage KubeCoder edges a single-stage run does not (slice 024 close-out B1, now
  ARCH-13) — the check already accounts for that.
- **G6. ArgoCDDeploy as a generated producer — probed.** A wrapper chart around upstream `argo-cd`
  (10.3.3) plus `chart/templates/webhook-relay.yaml`; one stage, `config/prd/`; deploys to
  namespace `argocd-prd`; `.kubecoder/project.yaml` says there is deliberately no Jenkinsfile. A
  throwaway clone with a judgment layer mapping `argocd` → owned `ss:argo-cd`, `redis` → owned
  `ss:redis`, and `webhook-relay` → `app:webhook-relay` with both `upstream` wires
  (`ARGOCD_WEBHOOK_URL` → `[server]`, `APPLICATIONSET_WEBHOOK_URL` → `[applicationset-controller]`)
  rendered `15 elements, 24 relations`, zero gaps, and `arch-validate` accepted it (run from source
  at `ff7e443`). The render yields application-controller, applicationset-controller,
  notifications-controller, repo-server (+ init `copyutil`), server, redis and webhook-relay; no
  dex (disabled in prd values). Both relay receivers are in-render Services
  (`chart/templates/webhook-relay.yaml:44-46`). `app:webhook-relay` is in the published set under
  `docker-images`. The probe's mappings prove the mechanism only: what the judgment layer models —
  whether Argo CD's bundled redis earns an owned element under the manual's "stable external
  identity" rule, a `webUi` entry for Argo CD's UI hosts — is the phase's call against the
  producer manual. An owned product's id is a uuid5 of its natural key under the shared namespace
  constant, so a second producer minting the same `ss:` element later collides loudly at the
  collector — acceptable, and worth one line in the how-to.
- **G7. The run starts only after the list form is live.** `ff7e443` is committed to ArgoCDTools
  `main`, unpushed at seeding. A deploy repo's local gate reaches the generator through this
  environment's `aac-tools` toolchain container, which only takes a new image on an environment
  restart. So before `/dev:run-slice`: the operator pushes ArgoCDTools, `IaC/ArgoCDTools` republishes
  `registry:5000/aac-tools:latest`, and the environment restarts. The ArgoCDDeploy phase's gate
  crashes on the list form otherwise.
- **G8. The collector.** `/work/Architecture/tooling/collect.py:380-392` raises on an id declared by
  two producers; the pipeline's collector stage runs before the viewer image build and the redeploy
  (`/work/Architecture/Jenkinsfile:98-150`), so a failed run publishes nothing. A registered
  producer with no artifacts hard-fails discovery (`collect.py` `discover_artifacts`, `:111-161`).
  `copyArtifacts` filters `'**/architecture/**/*.yaml'` (`Jenkinsfile:75`), so the archived path
  must contain an `architecture/` directory. The collector runs `--relaxed`.
- **G9. Registration.** `/work/Architecture/pipeline-producers.yaml` maps `id` → `jenkinsJob`;
  `kubecoder` is taken by the app repo (`AaC/KubeCoder`); there is no `kubecoder-deploy` or
  `argocd-deploy` entry and no `AaC/KubeCoderDeploy` or `AaC/ArgoCDDeploy` job. The producer manual
  is `/work/Architecture/.claude/architecture/producer-manual.md`; its "Generated producers" section
  (`:499`) requires `.architecturerc` with `generated: true` at the producer repo root and the
  `gap: <what>` console form.
- **G10. The container template.** `/work/JenkinsPipelineUtils/vars/containerTemplates.groovy`: every
  entry is `containerTemplate(name: name, image: …, command: 'sleep', args: 'infinity',
  alwaysPullImage: true)`; `python` floats (`registry:5000/python`). No `aac-tools` entry. The repo
  has no `.kubecoder/project.yaml` — no local gate.
- **G11. The existing producer pipelines.** `/work/HelmCharts/Jenkinsfile.architecture` (generate,
  validate, `archiveArtifacts 'docs/architecture/*.yaml'`, in a `k8s` container) and
  `/work/KubeCoder/Jenkinsfile.architecture` (validate + archive in `python`) are the shapes to
  follow; both `podTemplate(inheritFrom: 'jenkins-agent', …)` with `checkout scm`.
- **G12. Where the how-to lands.** `/work/Ansible/docs/runbooks/argocd.md` has
  `## Registering, undeploying and unregistering an app` (`:213`) and
  `## Previewing a migrating app's diff before its cutover` (`:246`); neither mentions an
  architecture step. No deploy-repo layout doc exists elsewhere.
- **G13. A stale record in DockerImages.** `/work/DockerImages/webhook-relay/architecture.yaml:14-17`
  says the relay's consumption edges are not modelled because *"neither Argo CD nor Fieldnotes is
  modelled by any producer yet"* and that ArgoCDDeploy *"is created by slice 009"*. Fieldnotes is
  modelled today (its own relay instance, by `helm-charts`), and after this slice Argo CD's edges are
  modelled on the instance by `argocd-deploy`. The comment is corrected in this slice. A DockerImages
  push rebuilds every image and runs its Helm deploy, so that repo is push-held.
- **G14. O2 in the `argo-cd` document set.** `/work/AnsibleSpecs/argo-cd/decisions.md` O2 ("What
  replaces HelmCharts' residual roles") and `design.md`'s "Coexisting with Jenkins during the
  migration" name `gen-architecture` as undecided; this slice decides that half — each deploy repo
  carries its own generated producer, run from the `aac-tools` image. `recommend-resources` and
  `collect-versions`/version-poller stay open.

## Ordering constraints

- **Before the run starts:** ArgoCDTools `ff7e443` is pushed, `registry:5000/aac-tools:latest` is
  republished, and this environment is restarted (G7). The operator's keystrokes, not a phase.
- KubeCoderDeploy commits its own `architecture.yaml` before the ArgoCDTools phase that makes the
  handover check read it (R7).
- The how-to and the `argo-cd` decision record land after the producers they describe.
- The whole slice lands before slice 012's **prd** flip; the dev flip does not wait on it.

## Push holds

- ../ArgoCDTools — pushing stays the operator's call, as in slice 024: a push to `main` makes `IaC/ArgoCDTools` rebuild and republish both images.
- ../DockerImages — a push rebuilds every image and runs the repo's Helm deploy; the one comment this slice corrects there goes out with the operator's next DockerImages push.

## Not in scope

- Creating the `AaC/KubeCoderDeploy` and `AaC/ArgoCDDeploy` Jenkins jobs, and the
  `pipeline-producers.yaml` registrations — owed to the operator (R5), in D2's order for KubeCoder.
- Creating KubeCoderDeploy's `prd` branch, the reconciler flips, and deleting HelmCharts'
  `charts/kubecoder/` — slice 012.
- Publishing KubeCoder's dev stage (R2).
- Any change to HelmCharts' generator beyond the pinning test (R4), including its four
  cross-stage KubeCoder edges (ARCH-13).
- Cross-app references resolved through the published set — slice 025.
- Migrating the estate's copies of `arch-validate.py` (ANS-78), the chart-name/registry-path check
  (ANS-85), and a test stage in the `IaC/ArgoCDTools` job (ANS-86).
- `recommend-resources` and `collect-versions`/version-poller, O2's other halves.
