/work/HomelabTerraformProvider — P3: git diff c1badcb8b70e..ce5b2bf9699c

.kubecoder/project.yaml |  8 ++++----
 Jenkinsfile             | 14 ++++++++++++++
 2 files changed, 18 insertions(+), 4 deletions(-)

diff --git a/.kubecoder/project.yaml b/.kubecoder/project.yaml
index 2113df4..42542c1 100644
--- a/.kubecoder/project.yaml
+++ b/.kubecoder/project.yaml
@@ -36,8 +36,8 @@ projects:
     # reaches no real infrastructure.
     test: cexec go go test ./...
 
-    # `go vet` because CI runs no linter at all — the Jenkinsfile's build stage
-    # is `go build` plus `go version -m` — and the repo carries no
-    # golangci-lint config. The `go` sidecar does ship golangci-lint; adopting
-    # it, and with which rule set, is a repo decision nobody has taken.
+    # `go vet` is the same check the Jenkinsfile runs before publish, beside
+    # the unit tests. The repo carries no golangci-lint config. The `go`
+    # sidecar does ship golangci-lint; adopting it, and with which rule set,
+    # is a repo decision nobody has taken.
     lint: cexec go go vet ./...
diff --git a/Jenkinsfile b/Jenkinsfile
index 6284655..2e622ca 100644
--- a/Jenkinsfile
+++ b/Jenkinsfile
@@ -53,6 +53,20 @@ podTemplate(inheritFrom: 'jenkins-agent-large', containers: [
             }
         }
 
+        // A vet or test failure fails the build here, so nothing reaches the
+        // registry. Must run after the build stage: vet and the test links
+        // are cgo and need the librados/librbd headers it installed into `go`.
+        // TF_ACC stays unset — the acceptance tests (TestAcc*) skip without
+        // it; they need live backends and are a manual run.
+        stage('Vet and unit tests') {
+            dir('HomelabTerraformProvider') {
+                container('go') {
+                    sh 'go vet ./...'
+                    sh 'go test ./...'
+                }
+            }
+        }
+
         // Append this build to the Provider Network Mirror (the dedicated
         // TerraformRegistry repo's dist/ tree) and push. That push triggers
         // the registry's pipeline, which rebuilds the nginx image and lets
