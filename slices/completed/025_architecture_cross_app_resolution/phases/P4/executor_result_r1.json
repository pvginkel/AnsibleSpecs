{"outcome": "done", "summary": "argo_migrate.py arch now reads one dataset snapshot on both sides (--dataset: a URL fetched once, or a file). Any loss or differing field on a kept id stops the app. HelmCharts' generator, rendering every release but the app's with nothing overlaid, must build and draw every 'drawn by helm-charts' edge exactly as published. Edges drawn by other producers are reported, not gated. Ansible f2db525 on phase/025-P4.", "refuted": []}
