{"outcome": "done", "summary": "Both scheduled jobs now run every stage. A failed prd stage appends its description entry and turns its stage and the build red via catchError around error(); the sh stays outside catchError. A failing dev stage raises notify.warning where it fails, and the post { unstable } blocks and DEV_STAGE_FAILED are gone. Committed as Ansible 1352e68; the root gate has no test statements, so it skips.", "refuted": []}
