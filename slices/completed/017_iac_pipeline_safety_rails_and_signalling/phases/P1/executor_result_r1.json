{"outcome": "done", "summary": "prevent_destroy = true is on the managed-vm VM resource's lifecycle block, covering every prd VM, and the three prd -replace comments now direct qm destroy on Proxmox then apply. The terraform gate (fmt -check) is green and terraform validate on the module passes; the no-op prd plan and a refused destroy are owed to iac-on-push and the operator. Code is b495c24 on phase/017-P1, and the done-record is committed in AnsibleSpecs.", "refuted": []}
