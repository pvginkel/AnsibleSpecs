{"outcome": "done", "summary": "HelmCharts 0c37dbb on phase/014-P6 adds one hermetic test that drives gen_architecture.main() over a synthetic tree. A release whose faked deploy config reports chart_name None is left out of the artifact, and the run still writes the artifact. The behaviour held, so there is no patch. Two mutations were witnessed red, and kc project test is green."}
