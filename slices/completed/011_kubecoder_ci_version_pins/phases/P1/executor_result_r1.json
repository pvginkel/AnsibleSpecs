{"outcome": "done", "summary": "KubeCoderDeploy's seven Build-Main pins now live only in config/{dev,prd}/values.yaml on build 523 — dev bare, prd prd-523 — with chart/values.yaml carrying no tag for them and the five container templates plus controller-config.yaml `required`-guarding their keys, so a stage missing one fails the render naming the key. tests/render-chart.py enforces the inverted invariant (chart names none, stage names exactly the seven, per-stage tag shape, one build within and across the stages, and a null-set render per key); twelve mutations each turn it red and `kc project test` and `kc project lint` are green. Committed as KubeCoderDeploy 49f0629 on phase/011-P1, plan done-record as AnsibleSpecs 5ceafee."}
