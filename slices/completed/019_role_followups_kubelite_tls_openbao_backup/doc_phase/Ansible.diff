/work/Ansible — P1: git diff bf11aa5a18a4..1a5117ac3ff8

ansible/roles/openbao/tasks/backup.yml               | 111 +++++++++++++++++++++++++++++++++++----
 ansible/roles/openbao/templates/backup-policy.hcl.j2 |  14 +++--
 2 files changed, 113 insertions(+), 12 deletions(-)

diff --git a/ansible/roles/openbao/tasks/backup.yml b/ansible/roles/openbao/tasks/backup.yml
index 2dc6422..3849e1a 100644
--- a/ansible/roles/openbao/tasks/backup.yml
+++ b/ansible/roles/openbao/tasks/backup.yml
@@ -16,6 +16,8 @@
 #   - site-openbao.yml Play 0 stages the upload token from
 #     `terraform output`.
 # This task reads those files back and delivers them to every node.
+# A staged secret_id outlives the rotation run that minted it, so each
+# node's pass proves it with an AppRole login before installing it.
 # Until all three inputs are resolvable the pipeline self-skips: no
 # timer, no failing unit.
 
@@ -45,17 +47,108 @@
     path: "{{ openbao_config_dir }}/backup-secret-id"
   register: _openbao_node_secret_id
 
+# The wrapper's own login, made against the leader-tracking VIP. Runs
+# under --check too. no_log would hide the uri module's failure
+# message, so the fail tasks below report the status and OpenBao's
+# error text instead.
+- name: Log in with the staged backup AppRole credentials
+  ansible.builtin.uri:
+    url: "{{ openbao_admin_api_addr }}/v1/auth/approle/login"
+    method: POST
+    body_format: json
+    body:
+      role_id: "{{ lookup('file', openbao_backup_staging_dir + '/openbao-backup-role-id') }}"
+      secret_id: "{{ lookup('file', openbao_backup_staging_dir + '/openbao-backup-secret-id') }}"
+    validate_certs: true
+  register: _openbao_backup_login
+  failed_when: false
+  no_log: true
+  check_mode: false
+  when:
+    - _openbao_staged_role_id.stat.exists
+    - _openbao_staged_secret_id.stat.exists
+
+# With no AppRole auth method mounted, OpenBao treats the login path as
+# an unauthenticated request and answers 403 "permission denied". A
+# mounted AppRole answers bad credentials itself, with 400 — so a dead
+# secret_id against a live cluster never reads as "no AppRole".
+- name: Classify the staged backup AppRole login
+  ansible.builtin.set_fact:
+    _openbao_backup_secret_id_proven: "{{ _openbao_backup_login.status | default(0) == 200 }}"
+    _openbao_backup_no_approle: >-
+      {{ _openbao_backup_login.status | default(0) == 403
+         and _openbao_backup_login.json.errors | default([]) == ['permission denied'] }}
+
+- name: Refuse a staged backup secret_id the backup AppRole rejects
+  ansible.builtin.fail:
+    msg: >-
+      The backup AppRole login with the staged role_id and secret_id in
+      {{ openbao_backup_staging_dir }} was rejected (HTTP 400:
+      {{ _openbao_backup_login.json.errors | default([]) | join('; ') }}),
+      so the staged secret_id was not installed. Mint and stage a fresh
+      one with a site-openbao.yml run with
+      -e openbao_rotate_secret_ids=true (it mints a fresh secret_id for
+      every AppRole).
+  when: _openbao_backup_login.status | default(0) == 400
+
+- name: Fail when the staged backup AppRole login did not complete
+  ansible.builtin.fail:
+    msg: >-
+      The backup AppRole login at {{ openbao_admin_api_addr }} with the
+      staged credentials returned HTTP {{ _openbao_backup_login.status }}:
+      {{ _openbao_backup_login.json.errors | default([_openbao_backup_login.msg | default('')]) | join('; ') }}.
+      The staged secret_id was not installed.
+  when:
+    - _openbao_backup_login.status is defined
+    - _openbao_backup_login.status not in [200, 400]
+    - not _openbao_backup_no_approle
+
+- name: Revoke the token the proving login returned
+  ansible.builtin.uri:
+    url: "{{ openbao_admin_api_addr }}/v1/auth/token/revoke-self"
+    method: POST
+    headers:
+      X-Vault-Token: "{{ _openbao_backup_login.json.auth.client_token }}"
+    status_code: 204
+    validate_certs: true
+  register: _openbao_backup_revoke
+  failed_when: false
+  no_log: true
+  check_mode: false
+  when: _openbao_backup_secret_id_proven
+
+- name: Fail when the proving login's token was not revoked
+  ansible.builtin.fail:
+    msg: >-
+      Revoking the backup AppRole token the proving login returned
+      failed: auth/token/revoke-self returned HTTP
+      {{ _openbao_backup_revoke.status }}:
+      {{ _openbao_backup_revoke.json.errors | default([_openbao_backup_revoke.msg | default('')]) | join('; ') }}.
+  when:
+    - _openbao_backup_secret_id_proven
+    - _openbao_backup_revoke.status != 204
+
 # Configurable once the role_id + upload token are staged AND the
-# secret_id can be placed — either it is staged now (a rotation run
-# just minted it) or a prior run already delivered it to the node.
+# secret_id can be placed — either a staged one was just proven, or a
+# prior run already delivered one to the node.
 - name: Decide whether the backup pipeline can be configured
   ansible.builtin.set_fact:
     _openbao_backup_ready: >-
       {{ _openbao_staged_role_id.stat.exists
          and _openbao_staged_token.stat.exists
-         and (_openbao_staged_secret_id.stat.exists
+         and (_openbao_backup_secret_id_proven
               or _openbao_node_secret_id.stat.exists) }}
 
+- name: Report the backup credential undelivered to a cluster without AppRole auth
+  ansible.builtin.debug:
+    msg: >-
+      Backup AppRole secret_id not delivered: the cluster has no AppRole
+      auth method to prove the staged one against, as on a whole-cluster
+      recovery converge before the snapshot restore. Converge
+      site-openbao.yml again after the restore to prove and deliver it.
+      {{ '' if _openbao_backup_ready else 'The backup pipeline stays unconfigured until then.' }}
+  when: _openbao_backup_no_approle
+
 - name: Skip the backup pipeline until its inputs exist
   ansible.builtin.debug:
     msg: >-
@@ -65,7 +158,9 @@
       backup secret_id) and the backup-server upload token
       (`terraform apply` for homelab_backup_credential). Re-run
       site-openbao.yml once those are in place.
-  when: not _openbao_backup_ready
+  when:
+    - not _openbao_backup_ready
+    - not _openbao_backup_no_approle
 
 - name: Configure the backup pipeline
   when: _openbao_backup_ready
@@ -85,10 +180,8 @@
         mode: "0400"
       no_log: true
 
-    # Delivered whenever a staged secret_id exists — a rotation run
-    # just staged a fresh one, or a prior rotation's file persists.
-    # When neither, _openbao_backup_ready already required the file to
-    # be on the node, so the wrapper still has a usable credential.
+    # Delivered only once proven by the login above. When not,
+    # _openbao_backup_ready required the file to be on the node already.
     - name: Deliver the backup AppRole secret_id
       ansible.builtin.copy:
         dest: "{{ openbao_config_dir }}/backup-secret-id"
@@ -96,7 +189,7 @@
         owner: openbao
         group: openbao
         mode: "0400"
-      when: _openbao_staged_secret_id.stat.exists
+      when: _openbao_backup_secret_id_proven
       no_log: true
 
     - name: Deliver the backup-server upload token
diff --git a/ansible/roles/openbao/templates/backup-policy.hcl.j2 b/ansible/roles/openbao/templates/backup-policy.hcl.j2
index e1bd2de..9b92236 100644
--- a/ansible/roles/openbao/templates/backup-policy.hcl.j2
+++ b/ansible/roles/openbao/templates/backup-policy.hcl.j2
@@ -2,9 +2,17 @@
 #
 # Read-only export scope for the daily backup. Covers a native Raft
 # snapshot plus a plaintext export of the policy catalog, auth
-# methods, mounts, and the full KV-v2 tree. No write capability
-# anywhere; the daily timer on each srvvaultN authenticates with this
-# AppRole and ships a `.tgz` to the backup-server.
+# methods, mounts, and the full KV-v2 tree. No write capability beyond
+# revoking its own token; the daily timer on each srvvaultN
+# authenticates with this AppRole and ships a `.tgz` to the
+# backup-server.
+
+# The AppRole sets token_no_default_policy, so revoke-self must be
+# granted here. backup.yml revokes the token of the login that proves a
+# staged secret_id.
+path "auth/token/revoke-self" {
+  capabilities = ["update"]
+}
 
 path "sys/storage/raft/snapshot" {
   capabilities = ["read"]

/work/Ansible — P2: git diff 1a5117ac3ff8..d305510141b6

ansible/roles/openbao/templates/openbao-backup.sh.j2 | 118 ++++++++++++++++++++++++++-------------
 1 file changed, 80 insertions(+), 38 deletions(-)

diff --git a/ansible/roles/openbao/templates/openbao-backup.sh.j2 b/ansible/roles/openbao/templates/openbao-backup.sh.j2
index 6d7d83d..e46d025 100644
--- a/ansible/roles/openbao/templates/openbao-backup.sh.j2
+++ b/ansible/roles/openbao/templates/openbao-backup.sh.j2
@@ -12,7 +12,9 @@
 #
 # Followers exit 0 immediately; only the Raft leader produces a bundle.
 # Any failure exits non-zero so the systemd unit is marked failed and
-# surfaces in `systemctl --failed`.
+# surfaces in `systemctl --failed`. A failed call logs its method, path,
+# HTTP status and OpenBao's error strings, never a response body: the
+# login, snapshot, KV and policy responses carry secret material.
 set -euo pipefail
 
 OPENBAO_ADDR="{{ openbao_api_addr }}"
@@ -25,8 +27,54 @@ UPLOAD_FILENAME="openbao-backup.tgz"
 
 log() { echo "openbao-backup: $*"; }
 
+work=$(mktemp -d /dev/shm/openbao-backup.XXXXXX)
+trap 'rm -rf "${work}"' EXIT
+resp="${work}/response"
+
+# request NAME OUT CURL_ARG... — one HTTP call: the body to OUT, the
+# status to ${http}. A call without a complete response ends the run.
+http=""
+request() {
+  local name="$1" out="$2" rc=0
+  shift 2
+  http=$(curl -sS -o "${out}" -w '%{http_code}' "$@") || rc=$?
+  if (( rc == 0 )); then
+    return 0
+  elif [[ "${http}" == "000" ]]; then
+    log "${name} failed: no HTTP response (curl exit ${rc})" >&2
+  else
+    log "${name} failed: HTTP ${http}, then curl exit ${rc}" >&2
+  fi
+  exit 1
+}
+
+# bao_request METHOD PATH OUT [CURL_ARG...] — one OpenBao API call.
+bao_request() {
+  local method="$1" path="$2" out="$3"
+  shift 3
+  request "${method} ${path}" "${out}" -X "${method}" "$@" \
+    "${OPENBAO_ADDR}/v1/${path}"
+}
+
+# bao_check METHOD PATH OUT — ends the run unless the last call answered
+# 2xx, logging the "errors" strings of OpenBao's error body in OUT.
+bao_check() {
+  local errors
+  if [[ "${http}" == 2?? ]]; then
+    return 0
+  fi
+  errors=$(jq -r '[(.errors? // [])[]? | strings | gsub("\\s+"; " ")
+    | ltrimstr(" ") | rtrimstr(" ")] | join("; ")' "$3" 2>/dev/null) \
+    || errors=""
+  log "$1 $2 failed: HTTP ${http}${errors:+: ${errors}}" >&2
+  exit 1
+}
+
+bao_api() { bao_request "$@"; bao_check "$1" "$2" "$3"; }
+
 # --- leader guard (sys/leader is unauthenticated) -------------------
-is_self=$(curl -fsS "${OPENBAO_ADDR}/v1/sys/leader" | jq -r '.is_self')
+bao_api GET sys/leader "${resp}"
+is_self=$(jq -r '.is_self' "${resp}")
 if [[ "${is_self}" != "true" ]]; then
   log "not the Raft leader; nothing to do"
   exit 0
@@ -35,30 +83,26 @@ fi
 # --- authenticate via the backup AppRole ----------------------------
 login_body=$(jq -n --arg r "$(< "${ROLE_ID_FILE}")" \
   --arg s "$(< "${SECRET_ID_FILE}")" '{role_id: $r, secret_id: $s}')
-token=$(curl -fsS -X POST -H 'Content-Type: application/json' \
-  -d "${login_body}" "${OPENBAO_ADDR}/v1/auth/approle/login" \
-  | jq -r '.auth.client_token')
+bao_api POST auth/approle/login "${resp}" \
+  -H 'Content-Type: application/json' -d "${login_body}"
+token=$(jq -r '.auth.client_token' "${resp}")
 if [[ -z "${token}" || "${token}" == "null" ]]; then
-  log "AppRole login returned no token" >&2
+  log "POST auth/approle/login returned no token" >&2
   exit 1
 fi
-
-bao_get() { curl -fsS -H "X-Vault-Token: ${token}" "${OPENBAO_ADDR}/v1/$1"; }
-
-work=$(mktemp -d /dev/shm/openbao-backup.XXXXXX)
-trap 'rm -rf "${work}"' EXIT
+auth=(-H "X-Vault-Token: ${token}")
 
 # --- the restore artifact: native Raft snapshot ---------------------
-curl -fsS -H "X-Vault-Token: ${token}" \
-  "${OPENBAO_ADDR}/v1/sys/storage/raft/snapshot" -o "${work}/raft.snap"
+bao_api GET sys/storage/raft/snapshot "${work}/raft.snap" "${auth[@]}"
 
 # --- collect ACL policies (name -> HCL text) ------------------------
 policies='{}'
-policy_names=$(curl -fsS -H "X-Vault-Token: ${token}" -X LIST \
-  "${OPENBAO_ADDR}/v1/sys/policies/acl" | jq -r '(.data.keys // [])[]')
+bao_api LIST sys/policies/acl "${resp}" "${auth[@]}"
+policy_names=$(jq -r '(.data.keys // [])[]' "${resp}")
 while IFS= read -r name; do
   [[ -n "${name}" ]] || continue
-  hcl=$(bao_get "sys/policies/acl/${name}" | jq -r '.data.policy // ""')
+  bao_api GET "sys/policies/acl/${name}" "${resp}" "${auth[@]}"
+  hcl=$(jq -r '.data.policy // ""' "${resp}")
   policies=$(jq -n --argjson o "${policies}" --arg n "${name}" \
     --arg p "${hcl}" '$o + {($n): $p}')
 done <<< "${policy_names}"
@@ -69,38 +113,32 @@ printf '%s\n' "${policies}" > "${work}/policies.json"
 # keys here" so a freshly-mounted, still-empty kv/ is not an error.
 kv='{}'
 kv_walk() {
-  local prefix="$1" listing http
-  listing=$(curl -sS -H "X-Vault-Token: ${token}" -X LIST \
-    -w '\n%{http_code}' \
-    "${OPENBAO_ADDR}/v1/${KV_MOUNT}/metadata/${prefix}")
-  http="${listing##*$'\n'}"
-  listing="${listing%$'\n'*}"
+  local prefix="$1" keys key
+  bao_request LIST "${KV_MOUNT}/metadata/${prefix}" "${resp}" "${auth[@]}"
   if [[ "${http}" == "404" ]]; then
     return 0
-  elif [[ "${http}" != "200" ]]; then
-    log "LIST ${KV_MOUNT}/metadata/${prefix} returned ${http}" >&2
-    return 1
   fi
-  local key
+  bao_check LIST "${KV_MOUNT}/metadata/${prefix}" "${resp}"
+  keys=$(jq -r '(.data.keys // [])[]' "${resp}")
   while IFS= read -r key; do
     [[ -n "${key}" ]] || continue
     if [[ "${key}" == */ ]]; then
       kv_walk "${prefix}${key}"
     else
-      local secret
-      secret=$(bao_get "${KV_MOUNT}/data/${prefix}${key}")
-      kv=$(jq -n --argjson o "${kv}" --arg p "${prefix}${key}" \
-        --argjson d "$(jq '.data.data' <<< "${secret}")" \
-        '$o + {($p): $d}')
+      bao_api GET "${KV_MOUNT}/data/${prefix}${key}" "${resp}" "${auth[@]}"
+      kv=$(jq --argjson o "${kv}" --arg p "${prefix}${key}" \
+        '$o + {($p): .data.data}' "${resp}")
     fi
-  done <<< "$(jq -r '(.data.keys // [])[]' <<< "${listing}")"
+  done <<< "${keys}"
 }
-kv_walk "" || { log "KV walk failed" >&2; exit 1; }
+kv_walk ""
 printf '%s\n' "${kv}" > "${work}/kv.json"
 
 # --- auth + mount config --------------------------------------------
-bao_get "sys/auth"   | jq '.data' > "${work}/auth.json"
-bao_get "sys/mounts" | jq '.data' > "${work}/mounts.json"
+bao_api GET sys/auth "${resp}" "${auth[@]}"
+jq '.data' "${resp}" > "${work}/auth.json"
+bao_api GET sys/mounts "${resp}" "${auth[@]}"
+jq '.data' "${resp}" > "${work}/mounts.json"
 
 # --- manifest -------------------------------------------------------
 jq -n \
@@ -114,11 +152,15 @@ jq -n \
 tar czf "${work}/${UPLOAD_FILENAME}" -C "${work}" \
   manifest.json auth.json mounts.json policies.json kv.json raft.snap
 
-curl -fsS -X POST \
+# backup-server answers in plain text, so only the status is logged.
+request "POST ${BACKUP_SERVER_URL}/upload" "${resp}" -X POST \
   -H "Authorization: Bearer $(< "${UPLOAD_TOKEN_FILE}")" \
   -H 'Content-Type: application/octet-stream' \
   --data-binary "@${work}/${UPLOAD_FILENAME}" \
-  "${BACKUP_SERVER_URL}/upload?filename=${UPLOAD_FILENAME}" \
-  > /dev/null
+  "${BACKUP_SERVER_URL}/upload?filename=${UPLOAD_FILENAME}"
+if [[ "${http}" != 2?? ]]; then
+  log "POST ${BACKUP_SERVER_URL}/upload failed: HTTP ${http}" >&2
+  exit 1
+fi
 
 log "backup uploaded (snapshot $(stat -c %s "${work}/raft.snap") bytes, bundle $(stat -c %s "${work}/${UPLOAD_FILENAME}") bytes)"

/work/Ansible — P3: git diff d305510141b6..e2e887b28706

ansible/roles/microk8s/handlers/main.yml | 26 ++++++++++++++++++++++++++
 1 file changed, 26 insertions(+)

diff --git a/ansible/roles/microk8s/handlers/main.yml b/ansible/roles/microk8s/handlers/main.yml
index 8284116..9e82693 100644
--- a/ansible/roles/microk8s/handlers/main.yml
+++ b/ansible/roles/microk8s/handlers/main.yml
@@ -15,6 +15,18 @@
   delay: 2
   changed_when: true
 
+- name: Report the microk8s restart a real run would perform (check mode)
+  # The restart handlers shell out, so --check skips them, and ansible.cfg's
+  # display_skipped_hosts = False hides even the skip. Each one covered has
+  # a debug handler listening on its name that reports the would-be restart
+  # as a change on every notified host instead. Only a changed task
+  # notifies, so a converged host still shows changed=0 under --check.
+  ansible.builtin.debug:
+    msg: A real run would restart microk8s on this node (microk8s stop, then start).
+  listen: Restart microk8s
+  changed_when: true
+  when: ansible_check_mode
+
 - name: Restart microk8s
   # Containerd reads certs.d/<host>/hosts.toml at start; editing the
   # file doesn't reach the running daemon. `microk8s stop && start`
@@ -26,6 +38,13 @@
     - microk8s start
   changed_when: true
 
+- name: Report the coredns rollout restart a real run would perform (check mode)
+  ansible.builtin.debug:
+    msg: A real run would rollout-restart kube-system/coredns.
+  listen: Rollout-restart coredns
+  changed_when: true
+  when: ansible_check_mode
+
 - name: Rollout-restart coredns
   # Same end-of-play apiserver-transient race as the Calico handler
   # above — retry the call rather than chasing a separate readiness
@@ -37,6 +56,13 @@
   delay: 2
   changed_when: true
 
+- name: Report the kubelite restart a real run would perform (check mode)
+  ansible.builtin.debug:
+    msg: A real run would restart kubelite on this node, one node at a time.
+  listen: Restart microk8s kubelite
+  changed_when: true
+  when: ansible_check_mode
+
 - name: Restart microk8s kubelite
   # kubelite is microk8s's combined apiserver/controller/scheduler/
   # proxy/kubelet process. Restarting just this unit picks up

/work/Ansible — P4: git diff e2e887b28706..d393616a664f

ansible/playbooks/renew-internal-tls.yml | 47 ++++++++++++++++++++++++++++---------------
 ansible/roles/microk8s/defaults/main.yml |  6 +++---
 ansible/roles/microk8s/handlers/main.yml | 57 ++++++++++++++++++++++++++++++++++++++--------------
 3 files changed, 76 insertions(+), 34 deletions(-)

diff --git a/ansible/playbooks/renew-internal-tls.yml b/ansible/playbooks/renew-internal-tls.yml
index a5e2399..2915dee 100644
--- a/ansible/playbooks/renew-internal-tls.yml
+++ b/ansible/playbooks/renew-internal-tls.yml
@@ -46,13 +46,13 @@
 #   ansible-playbook playbooks/renew-internal-tls.yml --limit '!k8s_dev'
 #   ansible-playbook playbooks/renew-internal-tls.yml --limit k8s_dev
 
-- name: Renew internal_tls leaf certificates approaching expiry
-  # One play across all three consumer groups rather than one play per
-  # group: when every host of a play fails, Ansible abandons the run and
-  # the later plays never execute (max_fail_percentage does not lift
-  # that), so one wedged group would cost the other groups their
-  # renewal. Within a single play a failing host only drops itself.
-  hosts: proxmox:k8s:openbao
+- name: Renew the pveproxy and OpenBao listener TLS leaves
+  # One play across both consumer groups rather than one play per group:
+  # when every host of a play fails, Ansible abandons the run and the
+  # later plays never execute (max_fail_percentage does not lift that),
+  # so one wedged group would cost the other its renewal. Within a
+  # single play a failing host only drops itself.
+  hosts: proxmox:openbao
   become: true
   gather_facts: true
   # A leaf is installed by one task and picked up by a handler at end of
@@ -69,9 +69,8 @@
   # `serial: 1` a batch is one host, so the first failed or unreachable
   # host leaves every later host untried. `max_fail_percentage` does not
   # buy it back; that check only ever breaks the play earlier. The
-  # one-at-a-time guarantee the kubelite restart and the OpenBao reload
-  # need is carried by those handlers' own `throttle: 1`, not by the
-  # driver.
+  # one-Raft-peer-at-a-time limit the OpenBao reload needs is carried by
+  # that handler's own `throttle: 1`, not by the driver.
   tasks:
     # Group membership is the only routing done here. Every other
     # condition on whether a host has a leaf lives in the task files
@@ -82,14 +81,30 @@
         tasks_from: internal_tls
       when: "'proxmox' in group_names"
 
-    - name: Renew the kube-apiserver homelab TLS leaf
-      ansible.builtin.import_role:
-        name: microk8s
-        tasks_from: internal_tls
-      when: "'k8s' in group_names"
-
     - name: Renew the OpenBao listener TLS leaf
       ansible.builtin.import_role:
         name: openbao
         tasks_from: internal_tls
       when: "'openbao' in group_names"
+
+- name: Renew the kube-apiserver homelab TLS leaves, one node at a time
+  hosts: k8s
+  become: true
+  gather_facts: true
+  # For the same reason as the play above.
+  force_handlers: true
+  # The exception to the play above. A re-issued leaf restarts kubelite,
+  # and a node that does not come back ready must stop the roll before
+  # the next node's control plane goes down. The restart handler's
+  # `throttle: 1` does not do that alone: in an un-serialised play, once
+  # one host fails the handler, the other notified hosts still restart,
+  # with or without any_errors_fatal. The cost of `serial: 1` is the one
+  # the play above avoids — the first failed or unreachable k8s node
+  # leaves every later k8s node unrenewed that run. A batch with every
+  # host failed also ends the whole run, which is why this play is last.
+  serial: 1
+  tasks:
+    - name: Renew the kube-apiserver homelab TLS leaf
+      ansible.builtin.import_role:
+        name: microk8s
+        tasks_from: internal_tls
diff --git a/ansible/roles/microk8s/defaults/main.yml b/ansible/roles/microk8s/defaults/main.yml
index 7750e00..49c2d44 100644
--- a/ansible/roles/microk8s/defaults/main.yml
+++ b/ansible/roles/microk8s/defaults/main.yml
@@ -248,9 +248,9 @@ microk8s_watchdog_randomized_delay: 120
 microk8s_apiserver_homelab_sans: []
 
 # How long the kubelite restart handler waits for the node it just
-# restarted to answer on its readiness endpoint before failing it. The
-# handler's one-node-at-a-time slot is held for that long, so this also
-# bounds what a wedged node costs the rest of the roll. update-k8s.yml
+# restarted to be ready again before failing it. The handler's
+# one-node-at-a-time slot is held for that long, so this also bounds
+# what a wedged node costs the rest of the roll. update-k8s.yml
 # budgets 2 minutes of retries for the same kubelite restart under
 # dqlite load.
 microk8s_kubelite_ready_timeout: 180
diff --git a/ansible/roles/microk8s/handlers/main.yml b/ansible/roles/microk8s/handlers/main.yml
index 9e82693..3255cc9 100644
--- a/ansible/roles/microk8s/handlers/main.yml
+++ b/ansible/roles/microk8s/handlers/main.yml
@@ -73,31 +73,58 @@
   #
   # The one-node-at-a-time guarantee is carried here rather than left to
   # each caller's `serial:`: throttle 1 hands the restart to one host at
-  # a time and the wait holds that host's slot until its kubelite answers
-  # again, so the next node goes down only once this one is serving.
+  # a time and the wait holds that host's slot until the node is ready
+  # again. A node not ready within microk8s_kubelite_ready_timeout fails
+  # the handler, which stops the roll only in a `serial: 1` play: in an
+  # un-serialised one the other notified hosts still take their turn.
   # Restart and wait must stay a single task — Ansible runs a handler
-  # across every notified host before starting the next handler, so split
-  # in two, every node would restart before any was waited on.
+  # across every notified host before starting the next handler, so
+  # split in two, every node would restart before any was waited on.
   #
-  # The endpoint is the one kubelite itself binds on this node class: an
-  # apiserver node's 16443, and on a worker the kubelet healthz port,
-  # because there 16443 belongs to the separate apiserver-proxy daemon
-  # and would answer while the local kubelite is still down.
+  # Ready depends on the node class:
+  # - An apiserver node: its own apiserver passes /readyz on 16443, which
+  #   covers the datastore and informer sync that /livez leaves out.
+  # - A worker runs no apiserver, and its 16443 is the separate
+  #   apiserver-proxy daemon, which answers while kubelite is still down.
+  #   Its Node object, read through that proxy with the kubelet's own
+  #   credential (the Node authorizer lets a kubelet read its own Node),
+  #   has to show Ready=True with a heartbeat later than the second the
+  #   restart returned. The Ready condition written before the restart
+  #   stays True until the node controller marks the node lost. A
+  #   restarted kubelet reports its status on its first sync, stamped by
+  #   this node's clock, and `systemctl restart` returns only once the
+  #   old kubelet has exited.
   ansible.builtin.shell:
     cmd: |
       set -eu
       systemctl restart snap.microk8s.daemon-kubelite
-      deadline=$(( $(date +%s) + {{ microk8s_kubelite_ready_timeout }} ))
-      until curl -sfk -m 5 -o /dev/null {{ _microk8s_kubelite_ready_url }}; do
+      restarted=$(date +%s)
+      deadline=$(( restarted + {{ microk8s_kubelite_ready_timeout }} ))
+      {% if microk8s_worker_only | bool %}
+      awaited="Ready report from node {{ inventory_hostname }} after the restart"
+      ready() {
+        seen=$(/snap/microk8s/current/kubectl \
+            --kubeconfig /var/snap/microk8s/current/credentials/kubelet.config \
+            get node {{ inventory_hostname }} \
+            -o 'jsonpath={range .status.conditions[?(@.type=="Ready")]}{.status} {.lastHeartbeatTime}{end}') \
+          || return 1
+        set -- $seen
+        [ "${1:-}" = True ] && [ "$(date -d "${2:-}" +%s)" -gt "$restarted" ]
+      }
+      {% else %}
+      awaited="HTTP 200 from https://127.0.0.1:16443/readyz"
+      ready() {
+        seen=$(curl -sk -m 5 -o /dev/null -w 'HTTP %{http_code}' https://127.0.0.1:16443/readyz) || :
+        [ "$seen" = "HTTP 200" ]
+      }
+      {% endif %}
+      seen=
+      until ready; do
         if [ "$(date +%s)" -ge "$deadline" ]; then
-          echo "kubelite did not answer {{ _microk8s_kubelite_ready_url }} within {{ microk8s_kubelite_ready_timeout }}s" >&2
+          echo "kubelite restarted, but got no $awaited within {{ microk8s_kubelite_ready_timeout }}s (last seen: $seen)" >&2
           exit 1
         fi
         sleep 2
       done
-  vars:
-    _microk8s_kubelite_ready_url: >-
-      {{ 'http://127.0.0.1:10248/healthz' if microk8s_worker_only | bool
-         else 'https://127.0.0.1:16443/livez' }}
   changed_when: true
   throttle: 1

/work/Ansible — P5: git diff d393616a664f..cd2f50d80d03

docs/runbooks/internal-tls-expiry.md | 190 +++++++++++++++++++++++++++++++++++++++++++++++++++++++
 docs/runbooks/openbao.md             |   3 +-
 2 files changed, 192 insertions(+), 1 deletion(-)

diff --git a/docs/runbooks/internal-tls-expiry.md b/docs/runbooks/internal-tls-expiry.md
new file mode 100644
index 0000000..25a3f54
--- /dev/null
+++ b/docs/runbooks/internal-tls-expiry.md
@@ -0,0 +1,190 @@
+# Runbook: expired internal_tls leaf certificate
+
+The X.509 counterpart of [`ssh-host-cert-expiry.md`](ssh-host-cert-expiry.md).
+
+Commands run from the Ansible checkout, as in that runbook. In a KubeCoder environment the toolchain
+lives in the `iac` sidecar, so prefix each command with `cexec iac` (see
+[`../live-infra-access.md`](../live-infra-access.md)).
+
+## Symptom
+
+An `internal_tls` leaf is the homelab-CA certificate a service presents, issued by step-ca through
+the `internal_tls` role. Once it lapses, every client that verifies it refuses the connection. Go
+clients (`bao`, `kubectl`, `step`) print:
+
+```
+tls: failed to verify certificate: x509: certificate has expired or is not yet valid: current time … is after …
+```
+
+curl prints `SSL certificate problem: certificate has expired`. Python clients print
+`certificate verify failed: certificate has expired`.
+
+| Consumer | Where the leaf is served | Who notices |
+|---|---|---|
+| Proxmox web UI (`proxmox_host`) | `https://<node>.home:8006` on pve, pve1 and pve2, from `/etc/pve/local/pveproxy-ssl.pem`. Each node has its own leaf. | Browsers and API clients of that node. |
+| `kubernetes-api.home` (`microk8s`) | `https://kubernetes-api.home:16443`, the keepalived VIP over srvk8s1–3. On srvk8sdev the name is `kubernetes-api-dev.home`. The leaf is `/var/snap/microk8s/current/certs/homelab-api.crt`. srvk8s4 is worker-only and has none. | Only clients that connect by that name. The apiserver serves the leaf only to SNI matching its SANs; kubelet, in-cluster traffic and clients using an IP or another name get microk8s's own `server.crt`. A lapsed leaf on a node not holding the VIP stays hidden until the VIP moves there. |
+| OpenBao listener (`openbao`) | `https://secrets.home` (443, passed through to 8200) and `https://srvvaultN.home:8200` on srvvault1–3, from `/etc/openbao/tls/tls.crt`. | Every OpenBao client: `bao`, the web UI, ESO, Jenkins `withVault`, and every `iac` run. **This breaks the usual recovery path** — see [The OpenBao listener leaf](#the-openbao-listener-leaf-openbao-is-unreachable). |
+
+## Cause
+
+Leaves live **47 days**, and `internal_tls` re-issues a leaf once fewer than **14** days are left.
+`IaC/Scheduled Certs` (Fridays, `Jenkinsfile.iac-scheduled-certs`) runs
+`playbooks/renew-internal-tls.yml` over every leaf, which gives two attempts inside that window. The
+daily `iac-scheduled-drift` build reds once a leaf is under 7 days. So a lapse means the Friday job
+failed or did not run on two Fridays in a row — **check it before doing anything else**. The build
+description says which stage broke:
+
+- `host certs may lapse; TLS leaf renewal did not run` — the SSH host-cert stage failed first, so
+  the TLS stage never ran. Step 1 below matters most here. An expired OpenBao listener leaf gives
+  this description too, because every stage fails at its first `iac -c`.
+- `internal_tls leaves may lapse` — the TLS stage itself failed; its console names the host and task.
+
+## 1 — Confirm the host is reachable over SSH
+
+The renewal runs over Ansible's SSH, so an expired SSH host certificate fails it before any
+certificate task. The host-cert stage runs first, and SSH host certificates also last 47 days, so
+a lapsed leaf often comes with a lapsed host certificate.
+
+```sh
+cd ansible && poetry run ansible 'proxmox:openbao:k8s' -m ping
+```
+
+- `pong` — go on.
+- `UNREACHABLE` with `Certificate invalid: expired` — fix the host certificate first with
+  [`ssh-host-cert-expiry.md`](ssh-host-cert-expiry.md), then come back. Its fix covers only VMs
+  Terraform builds from scratch, not pve, pve1 or pve2.
+- srvk8sdev is powered off most of the time; a connection timeout there only means it is off.
+
+## 2 — Confirm the leaf has lapsed (read-only)
+
+Read the certificate the service is actually serving. This works on an expired leaf, and needs no
+login:
+
+```sh
+openssl s_client -connect pve1.home:8006 -servername pve1.home </dev/null 2>/dev/null \
+    | openssl x509 -noout -subject -enddate
+```
+
+Use the same pair of options for the other consumers:
+
+| Consumer | `-connect` | `-servername` |
+|---|---|---|
+| Proxmox | `<node>.home:8006` | `<node>.home` |
+| kube-apiserver | `srvk8sN.home:16443` | `kubernetes-api.home` (on srvk8sdev: `kubernetes-api-dev.home`) — without it the apiserver serves microk8s's own certificate |
+| OpenBao | `srvvaultN.home:8200` | `secrets.home` |
+
+A `notAfter` in the past means the leaf has lapsed. One inside 14 days is due, and the next renewal
+run issues it.
+
+## 3 — Renew
+
+Once the host is reachable, a plain renewal run recovers the lapsed leaf. If the lapsed leaf is
+OpenBao's, use [the section below](#the-openbao-listener-leaf-openbao-is-unreachable) instead.
+
+```sh
+cd ansible && poetry run ansible-playbook playbooks/renew-internal-tls.yml --limit <host>
+```
+
+Starting an `IaC/Scheduled Certs` build does the same across the fleet, once its host-cert stage
+passes.
+
+**Why a plain run is enough.** The role issues a new leaf when `step certificate needs-renewal
+--expires-in 336h` exits 0, and that command also exits 0 for a leaf that has already expired.
+Issuance is a fresh `step ca certificate` authorised by a JWK token the controller mints. The old
+leaf is not needed, so its expiry does not stand in the way.
+
+**What the run needs.**
+- step-ca at `https://ca.home`: the controller mints the token there, and the host fetches its leaf
+  there. `curl https://ca.home/health` answers `{"status":"ok"}`. step-ca runs on the prd Kubernetes
+  cluster and needs nothing from OpenBao.
+- The ansible-vault passphrase: the JWK provisioner password is vaulted in
+  `inventories/prd/group_vars/all/vips.yml`.
+- The `step` CLI on the controller.
+
+**The playbook's two plays.**
+- The first play renews the pveproxy and OpenBao leaves, all hosts at once.
+- The last play renews the k8s leaves under `serial: 1`. It stops at the first k8s node that fails
+  or is unreachable, and the nodes after it are not reached. `--limit <node>` reaches a lapsed leaf
+  on one of those.
+- If every proxmox and openbao host fails, the k8s play never runs; `--limit k8s` runs it on its own.
+- srvk8sdev takes `--limit k8s_dev`.
+
+**What a good run shows.** The host reports `changed` on `Issue the leaf certificate on the target
+host`, then its handler runs: `Reload pveproxy`, `Reload openbao` or `Restart microk8s kubelite`.
+Repeat step 2; `notAfter` is now about 47 days out.
+
+A k8s node's `Restart microk8s kubelite` waits for the node to come back ready. If it fails with
+`kubelite restarted, but got no … within …s`, the new leaf is already on disk; the node's readiness
+is what failed, not the certificate.
+
+Then find out why the Friday job did not renew it, or you will be back here in 47 days.
+
+## The OpenBao listener leaf: OpenBao is unreachable
+
+Every `iac` run logs in to OpenBao before anything else, to resolve the `!bao` refs in srviac's
+`/etc/iac/secrets.yaml`. A lapsed listener leaf fails that login, and the container exits:
+
+```
+iac-impl: OpenBao AppRole login failed: … certificate verify failed: certificate has expired …
+```
+
+That stops every stage of `IaC/Scheduled Certs`, and any hand `iac -c` on srviac. Until this leaf is
+renewed, **nothing else in the fleet renews either**, so renew it first.
+
+The renewal itself reads nothing from OpenBao. Only the controller's credentials can depend on it.
+Besides step-ca and the `step` CLI, the controller needs two things:
+- the `ansible` SSH key, which reaches srvvault1–3;
+- the ansible-vault passphrase.
+
+On srviac the SSH key is a `!bao` ref (`kv/iac/ansible-ssh-key#private`); the vault passphrase is a
+literal. Two controllers have both without asking OpenBao:
+
+**A KubeCoder environment for this repo whose keys are already on disk.** Its setup
+(`scripts/kubecoder-keys.sh`) wrote them to `~/.ssh/id_ed25519_ansible` and `~/.ansible/vault-pass`,
+and the environment points `ANSIBLE_VAULT_PASSWORD_FILE` at the second file. The KubeCoder secret
+catalog is itself filled from OpenBao (`kv/eso/prd/kubecoder/prd/catalog`, per
+`.kubecoder/config.yaml`), so use an environment that already has both files. Don't set up a new one
+during the outage.
+
+```sh
+ls ~/.ssh/id_ed25519_ansible ~/.ansible/vault-pass
+cd ansible && cexec iac poetry run ansible-playbook playbooks/renew-internal-tls.yml --limit openbao
+```
+
+On 2026-09-14 such an environment reached pve, pve1, pve2, srvvault1–3 and srvk8s1–4 over SSH
+with those keys, and `https://ca.home/health` answered.
+
+**srviac, under the cold-boot procedure.** Follow [`iac-cold-boot.md`](iac-cold-boot.md) steps 1–4.
+They replace every `!bao` ref with its Roboform literal, the SSH key included. With no `!bao` ref
+left, `iac-impl` makes no OpenBao login. Then:
+
+```sh
+sudo iac -c 'cd /work/Ansible/ansible && ansible-playbook playbooks/renew-internal-tls.yml --limit openbao'
+```
+
+**After either run:**
+1. `Reload openbao` sends SIGHUP to one peer at a time. Repeat step 2 against srvvault1–3 and
+   `secrets.home:443`.
+2. If you used srviac's cold boot, do [`iac-cold-boot.md`](iac-cold-boot.md) step 5 now that
+   OpenBao answers.
+3. Start an `IaC/Scheduled Certs` build. Every build since the leaf lapsed failed before renewing
+   anything, so SSH host certificates and the other leaves may be due or lapsed as well. Run step 1
+   against the fleet first.
+4. What ESO and Jenkins did while OpenBao was unreachable, and how to bridge them, is in
+   [`openbao.md`](openbao.md) "Consumer cold-boot".
+
+## Notes
+
+- **`--check` previews but cannot fix.** Issuance is a `command`, which check mode skips. A
+  `--check` run reports `<cert path> needs (re)issuing — missing=…, within renewal window=…,
+  SAN drift=…` as changed for each due leaf, and issues nothing.
+- **A lapsed leaf is served, but the file on disk is fresh.** Step 2 shows the lapsed leaf, but a
+  `--check` run reports nothing due for that host. The service never reloaded the new leaf,
+  typically because a run was killed between issuing and its handler. A plain run finds nothing
+  due and reloads nothing. Force a new leaf, and the reload with it, by adding
+  `-e internal_tls_renewal_threshold_days=48` to the `--limit <host>` run. Above the 47-day life,
+  every leaf in scope counts as due.
+- **srvk8sdev** is off more than it is on, and its leaf lapses while it is off. That is the accepted
+  cost `Jenkinsfile.iac-scheduled-certs` records. Renew it with `--limit k8s_dev` when it is back.
+- Renewal design: the header of `ansible/playbooks/renew-internal-tls.yml` and
+  `ansible/roles/internal_tls/README.md`.
diff --git a/docs/runbooks/openbao.md b/docs/runbooks/openbao.md
index d6b4771..d5f439d 100644
--- a/docs/runbooks/openbao.md
+++ b/docs/runbooks/openbao.md
@@ -265,7 +265,8 @@ container on srviac, the Jenkins controller, and ESO inside each
 microk8s cluster. When OpenBao is unreachable (whole-cluster loss
 mid-recovery, network partition, listener cert expired), each one
 degrades differently. The right intervention depends on which one
-is on fire.
+is on fire. An expired listener cert is renewed per
+[`internal-tls-expiry.md`](internal-tls-expiry.md).
 
 ### iac-agent

/work/Ansible — P6: git diff cd2f50d80d03..31e86b528e00

docs/runbooks/openbao.md | 54 +++++++++++++++++++++++++++++++++++++++++++++++++++++-
 1 file changed, 53 insertions(+), 1 deletion(-)

diff --git a/docs/runbooks/openbao.md b/docs/runbooks/openbao.md
index d5f439d..2f32cbe 100644
--- a/docs/runbooks/openbao.md
+++ b/docs/runbooks/openbao.md
@@ -162,6 +162,12 @@ and the latest backup's Raft snapshot is restored into it.
    ssh srvvault1 sudo cat /dev/shm/openbao-init.json
    ```
 
+   This converge leaves the backup pipeline unconfigured. The empty
+   cluster has no AppRole auth to prove a staged backup secret_id
+   against, so it prints `Backup AppRole secret_id not delivered`, or
+   `OpenBao backup pipeline not configured` when none is staged.
+   Step 5 delivers it.
+
 4. **Restore the snapshot.** Copy `raft.snap` to `srvvault1`, then:
 
    ```bash
@@ -176,7 +182,43 @@ and the latest backup's Raft snapshot is restored into it.
    The fresh init file at `/dev/shm/openbao-init.json` is now stale —
    leave it; tmpfs clears on reboot and the keys it holds are inert.
 
-5. **Verify.**
+5. **Converge again, to deliver the backup credential.** The rebuilt
+   nodes hold no backup secret_id: step 3 had no `backup` AppRole to
+   prove one against. This converge authenticates with the restored
+   `openbao-admin` AppRole, re-stages the restored `backup` role_id,
+   and logs in with the staged secret_id before installing it:
+
+   ```bash
+   cd ansible && poetry run ansible-playbook playbooks/site-openbao.yml
+   ```
+
+   The outcome depends on the checkout's `tmp/openbao-backup-secret-id`:
+
+   - **Staged, and the restored role accepts it**: delivered to every
+     node, with `openbao-backup.timer` enabled.
+   - **Staged, and the restored role rejects it**: the run fails at
+     `Refuse a staged backup secret_id the backup AppRole rejects`,
+     naming `-e openbao_rotate_secret_ids=true`.
+   - **None staged** (a fresh clone, as every Jenkins run is): the
+     run completes with `OpenBao backup pipeline not configured`, and
+     no timer is installed.
+
+   For either of the last two, converge once more with the rotation
+   flag:
+
+   ```bash
+   cd ansible && poetry run ansible-playbook playbooks/site-openbao.yml \
+       -e openbao_rotate_secret_ids=true
+   ```
+
+   It mints and stages a fresh `backup` secret_id, which every node
+   proves and receives. It also mints a fresh never-expiring
+   secret_id for the other five AppRoles and revokes none, so every
+   consumer keeps its restored pair. The credentials it stages for
+   capture need no redistribution; wipe them with the `shred -u` its
+   closing message prints.
+
+6. **Verify.**
 
    ```bash
    export BAO_ADDR=https://secrets
@@ -186,6 +228,16 @@ and the latest backup's Raft snapshot is restored into it.
    bao kv get kv/<a known path>      # a real secret reads back
    ```
 
+   Run one backup on the node `list-peers` shows as leader:
+
+   ```bash
+   ssh srvvaultN sudo systemctl start openbao-backup.service
+   ssh srvvaultN sudo journalctl -u openbao-backup -n 5 --no-pager
+   ```
+
+   Expect `openbao-backup: backup uploaded (…)`. A failed run names
+   the call that broke.
+
    Consumers (iac-agent, Jenkins, ESO) need **no** credential
    redistribution — their AppRole `role_id`/`secret_id` pairs are
    part of the restored state.
