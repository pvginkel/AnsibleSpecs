{"outcome": "done", "summary": "Appended `iac/tf-backend` to `openbao_eso_kv_paths` in ansible/inventories/prd/group_vars/openbao.yml, with a block-comment paragraph stating why the prd eso AppRole reaches one leaf inside iac/ (one age keypair shared with iac, because both write into pvginkel/TerraformState — a copy under eso/prd/ would split on rotation). No other inventory or policy change: eso/prd/* already covers the git token leaf and every other inventoried leaf sits under shared/prd/* or eso/prd/*. `kc project test --project ansible` green; convergence remains the operator's site-openbao.yml run.", "cards": []}
