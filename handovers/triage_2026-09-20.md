# Triage 2026-09-20 — re-cut of slice 014 (architecture through the Argo CD migration)

Scope: the 2026-09-20 design session on slice 010's close-out S1, and backlog slice 014 (ANS-36).
Not the intake queue; no tracker card is an input. Raw material: `triage_2026-09-20_raw.md` (the
operator's messages are M1–M6 there). Items are chat passages, so they carry running ids and their
source is quoted in the block rather than inlined as card text.

Most items were **ruled in the session itself**; their `Ruling:` line quotes the operator's words
from the dump. What this pass still needs from you: the two `Question:` lines (C1's collision, C8), C4's
ruling, and the **Proposed sort** at the end.

Ruling vocabulary: `answer: …` (a new fact, the label is re-derived) · `override: <category>` ·
`remark: …` · `close` · `later` · `agreed` · `apply the suggested edit` ·
`conditional: <ruling> if <fact>` · `split: …` · `superseded by …`.

## Major

### C6 — The architecture model loses nothing through KubeCoder's cutover
- Source: M1; slice 010 close-out S1
- Ask: "The architecture file is a first class element the current HelmCharts setup. Actually, it's two parts: the static files and the generator. Both absolutely need to keep working as we switch over."
- Category: Major — "Once slice 012 lands, the published architecture model may lose the KubeCoder workload-to-product mapping."
- Note: the session found the loss happens at the registry flip (012 requirement 6), silently, not at requirement 13's deletion. With dev unpublished (C5) the constraint is on the **prd** flip only; 012 already carries that ordering line.
- Ruling: agreed in session — "This one is important. It reads like we have some optional thing that may break. That's not how I want to run this project."

**Card text:**
No card — chat passages (M1; slice 010 close-out S1) in `triage_2026-09-20_raw.md`; the quotes above are the operator's words from them.

## Feature

### C1 — The `aac-tools` image: generator and arch-validate as a container, built in ArgoCDTools
- Source: M2, M3, M4, M5
- Ask: "As for tools distribution I would suggest a container. Jenkins can pull those in and it means we have a fully managed system for this. It also means we don't need the scripts in the repo anymore."
- Category: Feature — "I'm now thinking the container should be some pipeline utilities thing, with one app being generating the architecture file or something like that."
- Collides: decisions.md, "Push pipelines check before they deploy or publish" (no id) — "Scanner and validator images are pinned by digest."
- Question: `aac-tools` carries `arch-validate`, a validator, and you ruled a floating tag. Does that line bind it — overrule the record for in-house tool images, or pin `aac-tools`? (The section it closes speaks of trivy and kubeconform, third-party images in push pipelines; whether it was meant to reach an image the estate builds itself is yours to say.)
- Ruling: agreed in session — place: "Put it in ArgoCDTools. The reason is that most of the complexity is around the Kubernetes based architecture generation stuff. The agent has that context in this repo. We can move it later if we want." · tag: "Yes on the floating tag." · name: "We're only putting AaC stuff in it. pipeline-utils invites a grab bag of different things. Yeah, go with aac-tools."

**Card text:**
No card — chat passages (M2, M3, M4, M5) in `triage_2026-09-20_raw.md`; the quotes above are the operator's words from them.

### C5 — KubeCoderDeploy's producer: prd only, from the `prd` branch, a required `--stage`; the static file moves
- Source: M2, M3, M4, M5, M6
- Ask: "Do we push architecture for dev? Implicitly, yes, today, but I have no need for it in my architecture manifest. Can't we just not publish dev architecture and only deploy architecture for kubecoder-prd?"
- Category: Feature — "As for the branch we describe that's based on the branch we use as a trigger for the Jenkins pipeline. If (and we should) use the prd branch, that means we're publishing the right thing."
- Ruling: agreed in session — not a generator rule: "Don't make it a generator rule. I may have different needs for other apps." · one stage per pipeline: "The artifact is attached to the pipeline. If the pipeline listens to dev and prd, the architecture would flap." · the guard: "I wasn't thinking it checks the branch name. It knows the stage, right? It has to build it to build the namespace. Can't we use that?" — settled as a required single-valued `--stage`, no branch check ("Go", M6; slice 014 ruling 3).

**Card text:**
No card — chat passages (M2, M3, M4, M5, M6) in `triage_2026-09-20_raw.md`; the quotes above are the operator's words from them.

### C7 — Cross-app references resolve through the published set
- Source: M2, M3
- Ask: "App A deploys partial architecture -> Publish aggregated set -> App B uses published set and builds its own -> Publish aggregated set -> App A can deploy its complete set"
- Category: Feature — "Cross app references will need to work in a different way. We always had the issue of circular references."
- Note: does not bite for KubeCoder (no cross-app edges in either direction). Owed before the first app with an in-cluster cross-app edge moves: the published set carries no in-cluster Service names today, and `helm-charts.yaml` holds 33 cross-release edges resolved in-process.
- Ruling: agreed in session — "We can do the same with our migration. We just have to make sure that we keep the published set in a valid state as we migrate stuff." · no second-pass trigger: "when we hit something like this for a new app, we manage this bootstrapping manually. It's not something we have to account for now."

**Card text:**
No card — chat passages (M2, M3) in `triage_2026-09-20_raw.md`; the quotes above are the operator's words from them.

### C3 — `aac-tools` as a KubeCoder catalog toolchain
- Source: M3, M4
- Ask: "if we want to be able to validate the generated architecture as part of local validation, we need to be able to generate the architecture locally."
- Category: Feature — "If we want to trial run the generation, we need it as a tool in KubeCoder just the same."
- Note: a change in the KubeCoder project's catalog, never made from this environment. It sits between the image (C1) and every repo's local gate (C5, C8, C4).
- Ruling: agreed in session — "I'd say it's a requirement."

**Card text:**
No card — chat passages (M3, M4) in `triage_2026-09-20_raw.md`; the quotes above are the operator's words from them.

## Improvement

### C2 — ArgoCDTools: one folder per image
- Source: M3, M4
- Ask: "I saw there's a Dockerfile in the root. Please rework that. Preference is to just name it Dockerfile.argocd-hook, but putting it in a folder is fine also. Maybe that depends on the shape of the new one."
- Category: Improvement — "Please rework that."
- Note: decisions.md's root-rotation inventory names `/work/ArgoCDTools/image/homelab-root.crt` as one of the copies the rotation runbook updates in lockstep; the folder move changes that path, so the slice updates the record. Not a collision — the decision records the path, it does not mandate it.
- Ruling: agreed in session — folders ("2. Yes.", M4, to: `argocd-hook/` holds its Dockerfile, `presync/`, `image/`, `tests/`; the new image gets a sibling folder, each its own kaniko context).

**Card text:**
No card — chat passages (M3, M4) in `triage_2026-09-20_raw.md`; the quotes above are the operator's words from them.

### C4 — Cross-repo scan for `arch-validate.py`, migrate to the toolchain
- Source: M3, M4
- Ask: "After we add the toolchain, we need to do a cross repo scan for the arch-validate.py script and migrate repos over (be it removing the script alltogether, or to use the toolchain)."
- Category: Improvement — "There's this arch-validate.py script that's copied all over the place. That's already a painful problem."
- Note: spans every producer repo (38 registered producers), most outside this environment.
- Ruling: Create a card for this. I will action this separately.

**Card text:**
No card — chat passages (M3, M4) in `triage_2026-09-20_raw.md`; the quotes above are the operator's words from them.

## Decision

### C9 — Element UUIDs are kept across the handover (reverses slice 014 requirement 5)
- Source: M3
- Ask: (assistant's phrasing — no operator wording exists) keep the same uuid5 namespace constant and natural keys, so a moved element keeps its id and only its owner changes; inbound edges then never dangle, which is what keeps the published set valid through a handover.
- Category: Decision — "I agree on the rest."
- Ruling: agreed in session — "I agree on the rest." (M3, against the recommendation to keep them; slice 014's triage answer was "Re-mint is fine").

**Card text:**
No card — chat passages (M3) in `triage_2026-09-20_raw.md`; the quotes above are the operator's words from them.

### C8 — HelmCharts runs the generator from the `aac-tools` image too
- Source: M2, M3
- Ask: (assistant's phrasing — no operator wording exists) HelmCharts' `Jenkinsfile.architecture` installs the repo's own `deploy` CLI into the `aac-tools` container, as it does into the `k8s` container today, and runs the image's generator — one generator codebase, accepted on a byte-identical `helm-charts.yaml` before and after.
- Question: does HelmCharts switch to the image in this work, or keep its own copy of `gen_architecture.py` until HelmCharts is deleted? You said both "It also means we don't need the scripts in the repo anymore" and "Did you think I wanted to move all scripts over?" — the session recorded the switch (slice 014 ruling 4) without an explicit yes from you.
- Category: Decision — "Did you think I wanted to move all scripts over?"
- Note: adjacent, not colliding — argo-cd D43: "Meanwhile, prefer not to add new things to HelmCharts." The switch removes HelmCharts' copy of the generator rather than adding to it.
- Ruling: No. I must assume that it can keep working as is. Honestly, I'd prefer you patch it if you need changes in it. I want to limit the amount of work we do on that repo.

**Card text:**
No card — chat passages (M2, M3) in `triage_2026-09-20_raw.md`; the quotes above are the operator's words from them.

## Carried from slice 014, not re-adjudicated

Already triaged on 2026-08-15 and unchanged by the session: requirement 1 (each deploy repo gains a
`Jenkinsfile.architecture`), requirement 3 (both deploy repos plus the reusable pattern —
ArgoCDDeploy's producer included, with slice 009's close-out S8 folded in), requirement 4 (the
`AaC/<Repo>` Jenkins job and the `pipeline-producers.yaml` PR are owed to the operator, registration
after the first green build). Requirement 2 is met by slice 008 and owes a pinning test;
requirement 5 is reversed by C9.

## Proposed sort — for your ruling

Three slices instead of one, because the KubeCoder toolchain (C3) has to land **between** the image
and the repos that gate on it locally — so the image cannot share a slice with its consumers.

| | Slice | Items | Depends on |
| --- | --- | --- | --- |
| A | **new** — `aac-tools` in ArgoCDTools: folder per image, the generator extracted with a deploy-repo front-end, `arch-validate`, image + Jenkinsfile | C1, C2 | — |
| B | **014, re-scoped, keeps its number and ANS-36** — producers and consumers: KubeCoderDeploy's prd producer and static file, HelmCharts onto the image (if C8 is yes) plus the pinning test, ArgoCDDeploy's producer, the JenkinsPipelineUtils container template, the pattern how-to | C5, C6, C8, C9, 014's requirements 1, 3, 4 | A; C3; **before 012's prd flip** |
| C | **new** — cross-app resolution through the published set: providers publish in-cluster Service names, host resolution against the published set, the hand-passed partial run, HelmCharts gains the same fallback | C7 | A; before the first app with an in-cluster cross-app edge migrates |

Not slices:

- **C3** — a task in the **KC** project (the catalog is KubeCoder's, worked from its environment),
  filed once slice A's image exists, naming the image and the two commands. Your call whether I file
  it in KC or you do.
- **C4** — an ANS task in **Later**, to be pulled once the toolchain exists; it fans out across
  projects, so it becomes per-project cards then, not one slice here.
- **Slice 010 close-out S1** — yours to write in `close-out.md`; suggested: "Folded into slice 014
  (rulings 2026-09-20) and its re-cut; 012 gained the ordering line."

Ruling on the sort: —
