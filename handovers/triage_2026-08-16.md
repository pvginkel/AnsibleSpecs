# Triage — Ansible cards in Triage/Inbox — 2026-08-16

Adjudication pass over all 28 Ansible-tagged cards in the Triage board's Inbox. `#624` is a
residuals card carrying two independent asks, so it yields two items (`#624a`, `#624b`) — 29 items
in all. None of the 28 carried a rubric label, so every item is adjudicated fresh here.

Raw source dump: [`triage_2026-08-16_raw.md`](triage_2026-08-16_raw.md).

**Reading this document.** Items are grouped by verdict, most severe first, so a group *is* the
view you act on. Each block carries the ask verbatim, the label with the quote that justifies it,
and — where one exists — a question for you, a note on stakes the label hides, or a research line.
`**Card text:**` inlines the whole source so you never have to look anything up; headings inside
those blocks are demoted by two levels so they do not collide with this outline.

**How to rule.** Write your ruling on the item's `Ruling:` line, in your own words. These forms
have fixed behaviour:

*On the label:*

- **answer: …** — a new fact; the label is re-derived from source-plus-fact.
- **override: \<category\>** — your label, recorded as yours, not re-derived.
- **remark: …** — recorded verbatim; moves nothing unless it contains a fact.

*On the item's fate:*

- **close** — binding, the item dies. **later** parks it.
- **agreed** — the card as written, and any recommendation made here, to the normal route.
- **apply the suggested edit** — a ceiling: the literal change the card names, nothing beyond it.
- **conditional: \<ruling\> if \<fact\>** — the fact becomes a research line; the item comes back.
- **split: …** — the named part becomes its own item and its own card.
- **superseded by …** — the ruling replaces the ask; the card closes naming what supersedes it.

You may also add or strike `Research:` lines directly.

---

## Board state as written, 2026-08-16 (the seam)

Adjudication is complete and every verdict is on its card. **Phase two — sort, file, sweep, close
out — has not run**, by operator decision, so both working documents stay in place. A later session
resumes `/dev:triage` at step 6 from the labelled board.

All 28 cards were labelled additively: the `Ansible` owner tag plus exactly one rubric label
(`Chore` for #577, which sits outside the rubric). Verified on the board by listing, not from the
write results.

**Archived — 10** (each with a comment carrying the ruling):

| Card | Label | Ruling |
|---|---|---|
| #427 | Minor | close as accepted — the card's own fourth option |
| #51 | Minor | close; not causing a problem, moving to VictoriaLogs |
| #327 | Minor | close; not a problem in practice (research line struck unrun) |
| #582 | Corner case | close; re-derived from Minor on the research verdict |
| #624 | Minor | close; both items (624a, 624b) closed, card carries the more severe label |
| #622 | Corner case | close |
| #620 | Nit pick | close |
| #574 | Improvement | close; nothing is broken |
| #617 | Invalid | close as superseded by slice 009's R3 / V08 |
| #623 | Nit pick | close as nit, operator override; the board tracks status |

**Moved — 2:** #69 (Decision) to **Later**, parked as an open question. #577 (Chore) to **Operator
Actions**, as the card itself proposed.

**Rewritten in place — 2:** #573's description trimmed to outstanding items 1–3, per "trim the card
then to what's still outstanding". #457 retitled and rewritten to the on-demand `setup-env.sh`
shape, dropping its catalog-projection route. Both original texts survive in
`triage_2026-08-16_raw.md`.

**Still in Inbox — 16**, awaiting phase two: #127, #573, #625 (Major); #49, #506, #581 (Minor);
#616, #618, #619, #583 (Nit pick); #635 (Improvement); #457, #575, #576, #125 (Feature); #129
(Test gap).

**Straightforward work extracted — 5 cards.** #616, #619, #583, #618 and #576 have their doc-level
changes written up for a fresh session at
[`triage_2026-08-16_doc_changes.md`](triage_2026-08-16_doc_changes.md). Those five cards are
deliberately **left open** — they close when that work lands, not before.

**Raised at triage and left unfiled:** `/work/IaCAgent` still exists on disk (stale, last commit
~2026-06-13, still holding `bin/send_message.py`), while `/work/Ansible/CLAUDE.md` states it is no
longer cloned — commits `ce4b66c` and `1cc5486` assert exactly that. Either the checkout should go
or CLAUDE.md should be corrected; no card was created.

---

## Major

### #127 — TF safety rails — destroy guard, prevent_destroy, apply the checked plan — https://trello.com/c/iiFSFRZ9

- Source: card #127
- Ask: "guard jq misses default replace ordering; only srviac protected; zero prevent_destroy despite decisions.md:512-513 claiming it; checked plan ≠ applied plan."
- Category: Major — "zero prevent_destroy despite decisions.md:512-513 claiming it; checked plan ≠ applied plan"
- Note: the card rates itself urgent, and names slice 005 (backups) as "the other half — already authored, run it" — that slice's status is worth confirming before this one is grouped.
- Ruling: Agreed.

**Card text:** Bundle at AnsibleSpecs/change_requests/tf_safety_rails/. Review C1: guard jq misses default replace ordering; only srviac protected; zero prevent_destroy despite decisions.md:512-513 claiming it; checked plan ≠ applied plan. Fix jq (contains(["delete"])), extend protection (srvvault1-3+), add prevent_destroy, single iac invocation applying the saved plan, drop `|| true` in drift job. Slice 005 (backups) is the other half — already authored, run it. Urgent-rated. Run /write-slice when ready.

### #573 — OpenBao backup: harden the credential handoff and failure visibility — https://trello.com/c/UjQumz5e

- Source: card #573
- Ask: "`backup.yml` delivers the staged secret_id on `stat.exists` alone, never checking it against the live role. The staging dir is a persistent per-checkout `tmp/`, so one stale file installs a dead credential permanently." / "Followers exit 0 — a dead backup looks healthy fleet-wide. No freshness check anywhere."
- Category: Major — "Followers exit 0 — a dead backup looks healthy fleet-wide. No freshness check anywhere."
- Note: the acute failure is already fixed live (2026-08-13); what remains is the recurrence-and-detection half. The comment records "Zero successes on any node, ever" over three months — that is the data-loss exposure the Major rests on, not the current state. Item 4 is struck through in the source (done); items 1–3 stand.
- Ruling: Close as resolved. I executed the statement to delete the old secret.
- Question: destroying the orphaned accessor discharges the card's closing line, but items 1–3 are untouched by it — `backup.yml` still installs a staged secret_id on `stat.exists` alone (the mechanism that produced the three-month outage), the wrapper is still bare `curl -fsS` throughout, and followers still exit 0 with no freshness check anywhere. Close all of it, or close the accessor and keep the recurrence-and-detection half? Flagging once because the card is Major on data-loss exposure; your call stands either way.
- Ruling (round 2): "Ok, trim the card then to what's still outstanding." — the card stays open and Major; its description is rewritten down to items 1–3 (the `stat.exists` handoff, the opaque `curl -fsS` legs, followers exiting 0 with no freshness check). The fixed premise, the struck item 4 and the orphaned-accessor line come out; the original text is kept below a rule so the history stays legible.

**Card text:** Fixed 2026-08-13 — backups flow now (bundle 497820 bytes from srvvault2, 20:46). The premise of this card was wrong: the upload leg was never broken.

The 400 came from the AppRole login at `openbao-backup.sh.j2:38`, not the backup-server POST. Nodes held a current role\_id paired to a secret\_id from a retired role generation — the live role's only accessor dates to 2026-05-22, the nodes were written 2026-05-23 from a stale staging file. A rotation run re-minted it. Diagnosis in the comment.

Remaining work:

1. Root cause: `backup.yml` delivers the staged secret_id on `stat.exists` alone, never checking it against the live role. The staging dir is a persistent per-checkout `tmp/`, so one stale file installs a dead credential permanently.
2. Every leg of the wrapper is a bare `curl -fsS`, so a failure never says which call broke. Cost three months of misdiagnosis.
3. Followers exit 0 — a dead backup looks healthy fleet-wide. No freshness check anywhere.
4. ~~First run today 504'd at 69s; the retry succeeded at 46s. nginx fronts backup-server with no~~ `proxy_read_timeout` ~~set (default 60s) and~~ `client_max_body_size 500M`~~. Set~~ `proxy_read_timeout 600s` ~~— a deliberately high ceiling, since a slow backup should wait rather than fail. No retry in the wrapper.~~

Orphaned 2026-05-22 accessor still on the role.

**Comment — Jeeves, 2026-08-13T18:51:58.459Z:**

#### Investigation, 2026-08-13

**The 400 was never the upload.** Replayed the AppRole login on srvvault2 with the node's own on-disk credentials:

```
login HTTP: 400
login errors: ["invalid role or secret ID"]
```

`openbao-backup.sh.j2:38` is a bare `curl -fsS` against `/v1/auth/approle/login`, so it exits 22 with exactly the message that was read as the upload POST. The script never reached `tar`, let alone the upload.

**Ruled out, with evidence:**

- backup-server reachable from srvvault2 (10.2.1.7, health 200). Auth is checked before anything that can 400 — unauthenticated POST returns 401 "missing bearer token", bogus token 401 "invalid token".
- Only three 400s exist on `/upload`: missing filename, filename outside [A-Za-z0-9._-], and a nil-body branch unreachable in Go. The script sends `filename=openbao-backup.tgz`, which is valid — it could never have produced a 400.
- AppRole mount healthy: ESO logs in continuously via clustersecretstore `openbao-prd`.
- Not decay: `secret_id_ttl` and `secret_id_num_uses` are never set in `approle.yml`, so minted secret_ids never expire and are unlimited-use.

**Failure history follows Raft leadership**, which is why it looked like a single-node problem: srvvault1 Jun 5–Aug 1 (40 failures), srvvault3 Aug 2–Aug 8 (7), srvvault2 Aug 9–Aug 13 (5). Zero successes on any node, ever.

**Root cause.** The live role holds exactly one secret_id accessor: created 2026-05-22 11:24, no TTL, unlimited uses, no CIDR binding. The node credentials were written 2026-05-23 17:06. Since nothing here expires or revokes secret_ids, every mint against the current role generation would still be listed — so no mint happened on May 23. The value `backup.yml` wrote that day came from a leftover controller-side staging file, not from a mint. The role_id delivered alongside it was correct, because role_id is re-read from the API and re-staged every run. Result: valid role_id + dead secret_id = `invalid role or secret ID`, permanently.

**Fix applied.** `site-openbao.yml -e openbao_rotate_secret_ids=true`. No admin token needed — `auth-token.yml` falls back to the ansible-vault'd admin AppRole in `inventories/prd/group_vars/openbao.yml`; `openbao_admin_token` is the bootstrap/rescue override only, and the root token was retired long ago. The run is additive: nothing revokes existing secret_ids, so ESO, Jenkins and iac-agent were unaffected. `/etc/openbao/backup-secret-id` is now dated 2026-08-13 20:38, and a manual run at 20:46 logged `backup uploaded (snapshot 471414 bytes, bundle 497820 bytes)`.

**Follow-up.** Once the new credential has survived a few timer cycles, destroy the orphaned accessor:

```
bao write auth/approle/role/backup/secret-id-accessor/destroy secret_id_accessor=<the 2026-05-22 guid>
```

### #625 — srvk8s3: memory PSI counter wedged at 1.000 s/s — stuck-on alerts nobody receives — https://trello.com/c/zvUKmXfl

- Source: card #625
- Ask: "NodeMemoryStalled (critical) and NodeMemoryStallElevated (warning) have both been firing on srvk8s3 since ~2026-08-13, across 485 five-minute buckets." / "Alertmanager still has no receiver configured, so none of it was sent anywhere."
- Category: Major — "The detection layer added after the 2026-08-02 memory incident is currently stuck-on, and the \"nobody hears it\" half is what makes that dangerous rather than merely noisy."
- Note: the card names two coupled facets — a counter the rules trust that can wedge, and no delivery path at all. The second is the one that generalises past srvk8s3: no alert of any kind reaches anyone today.
- Ruling: Agreed.
- Cross-item note (from #125's research, recorded here because it bears on grouping): this card's "no receiver configured" half is not srvk8s3-specific and not new. `/work/DockerImages/docs/alert-manager/plan.md`, written 2026-08-12 and not implemented, documents the same state — "a single `default-receiver` with no receiver configuration at all… They fire and are silently discarded" — and plans the delivery path. #125's Alertmanager clause and its operator comment are the same thread. The wedged-counter half is this card's alone.

**Card text:** srvk8s3's memory PSI "some" counter advances at exactly 1.000 s/s — 1800.0s of stall per 1800s bucket — while the node reports 5.3 GiB MemAvailable and 2.2 major faults/s. That is not pressure; the counter is wedged.

NodeMemoryStalled (critical) and NodeMemoryStallElevated (warning) have both been firing on srvk8s3 since ~2026-08-13, across 485 five-minute buckets.

Alertmanager still has no receiver configured, so none of it was sent anywhere. The detection layer added after the 2026-08-02 memory incident is currently stuck-on, and the "nobody hears it" half is what makes that dangerous rather than merely noisy.

Two coupled facets: the alert rules trust a counter that can wedge, and there is no delivery path to notice when they misfire — or when they fire for real.

Split out of #412 (kube-reserved on the microk8s nodes), archived 2026-08-15 — its 08-02 execution shipped these alerts as HelmCharts `97fa810` + `9898d0a`. See that card's closing comment for the reservation-value and liveness-probe threads it left open.

---

## Minor

### #427 — k8s rolls leave pod placement badly skewed — no rebalance pass — https://trello.com/c/zspi6WDQ

- Source: card #427
- Ask: "`update-k8s.yml` drains one node at a time, and drained pods never come back. The last node in the roll returns nearly empty while the others stay packed." / "The concern is that this recurs on every roll, and `IaC/Scheduled Update` runs unattended at `H 4 * * 0` with nothing to correct it."
- Category: Minor — "Two of three nodes sat at 81% / 86% of allocatable requests, which defeats the headroom the resize was for."
- Note: outage-adjacent, and the label does not show it — the headroom being defeated is the headroom the 2026-08-02 memory incident bought, and the card states "an N-1 drain check computed on a skewed cluster is measuring an artifact", i.e. the safety gate itself is unreliable. Rule it Major if you read that as an outage risk rather than degraded resilience.
- Ruling: Close as accepted.

**Card text:** `update-k8s.yml` drains one node at a time, and drained pods never come back. The last node in the roll returns nearly empty while the others stay packed.

Observed on the 2026-08-02 memory roll (srvk8s1/2/3): afterwards srvk8s1 12797 Mi, srvk8s2 13599 Mi, srvk8s3 1208 Mi — an 11.3x spread against the <=1.5x criterion in AnsibleSpecs/handovers/memory-issues/07-capacity.md. Two of three nodes sat at 81% / 86% of allocatable requests, which defeats the headroom the resize was for.

Corrected by hand with `rollout restart` on five deployments (gitblit, registry, trello-mcp, jenkins, media), moving ~8.9 GiB and landing at 57/53/64% — a 1.21x spread.

The concern is that this recurs on every roll, and `IaC/Scheduled Update` runs unattended at `H 4 * * 0` with nothing to correct it. So the cluster spends most of each week skewed, and an N-1 drain check computed on a skewed cluster is measuring an artifact.

Options worth weighing:
- a descheduler (RemoveDuplicates / LowNodeUtilization strategies)
- a rebalance pass at the end of `update-k8s.yml` (restart the heaviest movable deployments once the last node rejoins)
- pod topology spread constraints on the heavier workloads
- accept it, and only rebalance before a measurement

Caveat for whichever wins: a `RollingUpdate` deployment on an RWO volume deadlocks on restart, and `Recreate` singletons cost real downtime — so a blind "restart everything" pass is not safe. Strategy and PVC access mode have to be checked per workload.

Context: AnsibleSpecs/handovers/memory-issues/HANDOVER.md

### #49 — srvvaultN: systemd-networkd-wait-online hangs ~120s on boot — https://trello.com/c/AaazOKxa

- Source: card #49
- Ask: "`systemd-networkd-wait-online.service` blocks for ~120s before timing out non-fatally, delaying SSH availability post-boot."
- Category: Minor — "Boot completes after the unit's TimeoutStartSec fires, so it's a delay-bug not a fail-stop, but adds 2 minutes to every reboot of every srvvaultN."
- Note: the card names three candidate fixes and marks the pick as yours — that choice is refinement work, not a triage question, so it rides into planning rather than being settled here.
- Ruling: Agreed.

**Card text:** Observed during card #8 reboot-test on srvvault1: `systemd-networkd-wait-online.service` blocks for ~120s before timing out non-fatally, delaying SSH availability post-boot. Boot completes after the unit's TimeoutStartSec fires, so it's a delay-bug not a fail-stop, but adds 2 minutes to every reboot of every srvvaultN.

Likely cause: the static netplan has an IPv6 global address (`2a10:3781:16a9:1::40/64` on srvvault1) configured with `accept_ra: false` and no static IPv6 default route, so networkd's "online" check waits for v6 routability that never materialises until timeout.

Three candidate fixes (operator pick):
- Add an explicit IPv6 default route per host (`routes:` entry in `network_devices`), matching the v4 gateway pattern. Most "correct" — gives the host a real v6 default.
- Configure `RequiredForOnline=routable:ipv4` on the networkd unit for hosts that don't need v6 routability. Downgrades the online check, keeps v6 link-local-only.
- Set `optional: true` on the v6 portion of the netplan (per-address). Skips wait-online's check for that address.

Affects every static-netplan host with v6 addresses + accept_ra: false (today: srvvault1/2/3, k8s_prd nodes too). Fix likely lives in the baseline role or in the cloud-init/netplan template Terraform writes. Not blocking card #9.

### #51 — ES logstash-http: find the writer, stop the shard sprawl at source — https://trello.com/c/EcEpYXPm

- Source: card #51
- Ask: "Find what writes `logstash-http-*` — there's no Logstash; an iotsupport app HTTP-appends directly to ES with a date-based daily index name." / "Confirm it doesn't recreate a competing index template that would override `logstash-http-retention` (reverting `replicas:0`)."
- Category: Minor — "ES pod `cpu` request is `20m` (starves recovery) and `bootstrap.memory_lock` is failing — both worth a HelmCharts pass."
- Note: the acute sprawl was live-fixed 2026-06-15 and committed; what remains is finding the unlocated writer, a regression risk against the applied mitigation, an ILM migration, and two standing ES pod defects. The writer lives in an app image outside this estate's charts, so item 1 may be an investigation rather than a change.
- Ruling: Close. It's not causing a problem plus I want to move to VictoriaLogs.

**Card text:** Live-fixed 2026-06-15: `logstash-http-*` was 269 daily indices / 538 shards (replica-on-single-node + 365d retention) — the bulk of the cluster's shards and a major ES slow-start cause. Applied `replicas:0` + 14d ILM retention live, and committed the policy/template bootstrap to `elasticsearch-setup` (DockerImages 2ec5c59).

Remaining:
1. Find what writes `logstash-http-*` — there's no Logstash; an iotsupport app HTTP-appends directly to ES with a date-based daily index name. Not in HelmCharts/iot chart (baked into an app image). Locate it.
2. Confirm it doesn't recreate a competing index template that would override `logstash-http-retention` (reverting `replicas:0`).
3. Migrate daily indices -> ILM rollover alias / data stream (roll at ~1 GB or 7d) to collapse ~14 tiny daily indices into one or two.
4. While there: ES pod `cpu` request is `20m` (starves recovery) and `bootstrap.memory_lock` is failing — both worth a HelmCharts pass.

Context: ES on single-node elasticsearch-prd, 1 GB heap. Guideline is <=20 shards/GB heap; logstash-http alone was ~530 over.

### #327 — iac shim: --pull=always turns a registry blip into a failed build — https://trello.com/c/kXcGQrOS

- Source: card #327
- Ask: "`/usr/local/bin/iac` (IaCAgent repo) runs `docker run --pull=always registry:5000/iac:latest` on *every* invocation, so any transient registry/DNS failure fails whatever pipeline stage it lands in."
- Category: Minor — "A green roll read as a red build."
- Note: the card's evidence includes the `post { failure }` notification step failing on the same blip — a build that fails on contention also cannot page. Its scope cites the retired `IaCAgent` repo path; the tree now lives at `support/iac-agent/`.
- Research: ~~does `support/iac-agent/bin/iac` still run `docker run --pull=always` with no retry, or did the slice 013 fold-in change it?~~ — struck, moot under the ruling.
- Ruling: Close. Not a problem in practice.

**Card text:** `/usr/local/bin/iac` (IaCAgent repo) runs `docker run --pull=always registry:5000/iac:latest` on *every* invocation, so any transient registry/DNS failure fails whatever pipeline stage it lands in.

IaC/Scheduled Update #10 (2026-06-28) is the evidence: the prd roll succeeded in full, then `lookup registry on 127.0.0.53:53: server misbehaving` broke the dev stage *and* the `post { failure }` notification step. Exit 125, build reported FAILURE. A green roll read as a red build.

Options: retry the pull a couple of times, or fall back to the cached image when the registry is unreachable (`--pull=missing`) — the current comment argues never to run a stale image, so a bounded retry is probably the better fit.

Cheap tell when triaging a red IaC build: exit code 125 means the shim, not the roll.

### #506 — Remove the /var/lock/iac.lock flock from bin/iac — https://trello.com/c/yzCrLvTB

- Source: card #506
- Ask: "The flock existed because `iac-impl` cloned TerraformState and did sync_state_in/out with no remote lock (decisions.md:503). That's gone — `terraform-backend-git` takes `locks/<state-path>` branches" / "`flock -w 60` fails rather than queues; a collision becomes a red build."
- Category: Minor — "`flock -w 60` fails rather than queues; a collision becomes a red build."
- Note: the card claims this is a "Likely blocker for Argo CD" — PreSync hooks would contend dev-vs-prd and fail at the hardcoded 60s. If that holds, ordering matters against the 009–012 backlog even though the severity is Minor. Its scope list also cites `IaCAgent/bin/iac` and `send_message.py`, both since moved or removed.
- Research: does `support/iac-agent/bin/iac` still take the `/var/lock/iac.lock` flock with `-w 60`?
- Research verdict: still there and unchanged — `support/iac-agent/bin/iac:45-55` takes `/var/lock/iac.lock` on fd 9 with `flock -w 60 -x`, and `exit 1`s on timeout rather than queueing. Nothing else in `support/iac-agent/` takes it; `bin/jenkins-agent-launch.sh:49,67` only bind-mounts `/var/lock` into the agent container so the host lock is visible. One of the card's three "why it hurts" bullets is now moot: `send_message.py` is gone, so the post-block paging path no longer takes the lock.
- Ruling: Agreed.

**Card text:** The flock existed because `iac-impl` cloned TerraformState and did sync_state_in/out with no remote lock (decisions.md:503). That's gone — `terraform-backend-git` takes `locks/<state-path>` branches, wired via lock_address/unlock_address in both backend.tf. As a state guard the flock is also illusory: wrkdev and the KubeCoder pod write the same store and it sees neither.

Cross-job serialisation, its other job, is already covered — the `IaC Agent` node is set to 1 executor, and it queues instead of failing.

Why it actively hurts:
- `flock -w 60` fails rather than queues; a collision becomes a red build.
- Post-block `iac -c 'send_message.py …'` takes the same lock, so a build failing on contention can't page.
- Global: HelmCharts takes it once per release (~55 `iac -c` calls); Argo PreSync hooks would contend dev-vs-prd and fail at 60s, with `-w 60` hardcoded in the shim (argo-cd/qa.md:704, review-fable.md R1). Likely blocker for Argo CD.

Accepted loss: hand-run Ansible on srviac no longer interlocks with a running job (terraform still does, via lock branches).

Scope: `IaCAgent/bin/iac` + README; decisions.md 130/503; docs/runbooks/iac-agent.md 11/41/51/120; Jenkinsfile header comments (on-push, certs, calico) + HelmCharts Jenkinsfile; argo-cd plan.md/qa.md.

### #581 — iac-image: the poller's weekly rebuild can still be skipped and never retried — https://trello.com/c/f1ZHXKa0

- Source: card #581
- Ask: "A poller-triggered build that happens to absorb commits touching no image input therefore still skips, and per the poller's bookkeeping that weekly rebuild is never retried — rebuild-at does not advance, and after STALE_GRACE the poller reports the image orphaned."
- Question: the card asks for a ruling — do we adopt the params-driven force path DockerImages uses, which slice 013 deliberately did not adopt? The card notes the original "no escape hatch" ruling weighed manual forcing, not the poller.
- Category: Minor — "A poller-triggered build that happens to absorb commits touching no image input therefore still skips, and per the poller's bookkeeping that weekly rebuild is never retried"
- Note: the card also records a test gap — verification.json had no criterion covering `Jenkinsfile.iac-image:37-39`, and the pure-skip path is unobservable from inside the slice. The defect label dominates, but the gap rides with it.
- Ruling: The issue more general. All pipelines that conditionally build the image likely won't based on the version poller signal. I see two options. Either this is always two stage: detect whether the image needs to be rebuilt, and then schedule a separate pipeline. That second pipeline is then picked up by version poller. The alternative is that we send a signal to the pipeline it can test for, e.g. the trigger field. The first option is cleaner, but more verbose. All pipelines need to be checked.

**Card text:** From slice 013 (PA reviewer r2 + completion consult) — needs an operator ruling.

Jenkinsfile.iac-image infers "the version poller triggered this" from an empty changeset. A poller-triggered build that happens to absorb commits touching no image input therefore still skips, and per the poller's bookkeeping that weekly rebuild is never retried — rebuild-at does not advance, and after STALE_GRACE the poller reports the image orphaned.

Narrower than the r1 blocker PA fixed, same failure mode. Closing it needs a ruling because the "no escape hatch" ruling weighed manual forcing, not the poller — DockerImages solves this with a params-driven force path that 013 deliberately did not adopt.

Also unverified: verification.json had no criterion covering Jenkinsfile.iac-image:37-39, and the test phase confirmed the pure-skip path is unobservable from inside the slice.

Slice: AnsibleSpecs/slices/completed/013_iac_pipeline_restructure/

### #582 — iac_agent rsync ships gitignored build artifacts to /opt/IaCAgent — https://trello.com/c/7SGDyHZH

- Source: card #582
- Ask: "ansible/roles/iac_agent/tasks/main.yml:88-95 rsyncs support/iac-agent with delete: true and only --exclude=.git. Any gitignored build artifact present in the operator's workstation checkout (__pycache__/ being the obvious one) ships to /opt/IaCAgent on srviac, and re-ships on every apply."
- Category: Minor — "Any gitignored build artifact present in the operator's workstation checkout (__pycache__/ being the obvious one) ships to /opt/IaCAgent on srviac, and re-ships on every apply."
- Ruling: I don't deploy iac_agent from my workstation. However, if this is really the fix described, then I'm all for it. Agreed as described (the --filter fix).
- Research verdict (on the conditional): the fix is real but the premise is thinner than the card claims. The task is `ansible/roles/iac_agent/tasks/main.yml:87-95`, an `ansible.posix.synchronize` whose `src` is `iac_agent_local_checkout` = `{{ playbook_dir }}/../../support/iac-agent` (`defaults/main.yml:7`) — so it pushes from whichever checkout runs `ansible-playbook`, not from a fixed path on the target. `rsync_opts` is exactly `--exclude=.git` and nothing else. `support/iac-agent/.gitignore` exists and holds `__pycache__/` and `*.pyc`, so `--filter=':- .gitignore'` would work as described. But **there is no Python under `support/iac-agent/` at all** — `iac-impl` and the check scripts are shell — so `__pycache__`, the card's named artifact, cannot be generated there; the tree is 11 tracked files and `git status` is clean. Separately, `site.yml:32-40` and `docs/runbooks/iac-agent.md:34-36,89-93` say the `iac_agent` role is only ever applied from wrkdev — the `iac-apply` job passes `--limit '!iac_agent'` — which contradicts the ruling's "I don't deploy iac_agent from my workstation".
- Category (re-derived on the verdict): Corner case — the named vector cannot arise, since no Python source exists in the synced tree to generate `__pycache__`.
- Question: two things. The one-line `--filter` is still cheap defence against some future artifact — keep it as a Corner-case fix, or close? And the runbook says wrkdev is the *only* path that applies this role; if you are not the one running it, the runbook is wrong and that is worth its own card.
- Ruling (round 2): "Then just close it please." — closes. Final category: Corner case. The runbook-versus-practice discrepancy is not carried by this card; it stays unfiled unless raised separately.

**Card text:** From slice 013 (PC advisory).

ansible/roles/iac_agent/tasks/main.yml:88-95 rsyncs support/iac-agent with delete: true and only --exclude=.git. Any gitignored build artifact present in the operator's workstation checkout (__pycache__/ being the obvious one) ships to /opt/IaCAgent on srviac, and re-ships on every apply.

Suggested fix: --filter=':- .gitignore', or an explicit __pycache__ exclude.

Slice: AnsibleSpecs/slices/completed/013_iac_pipeline_restructure/

### #618 — argo-cd/decisions.md D31: the hook image's contents list is stale — https://trello.com/c/liPx5qs2

- Source: card #618
- Ask: "D31 (:241-243) says the image carries \"Terraform, terraform-backend-git, git, and the presync scripts — nothing else\". librados2/librbd1, image/terraform.rc and image/homelab-root.crt all ride beyond that" / "nothing in the set points at the hook image as a consumer, so a sweep that rotates the step-ca root or readdresses tfmirror.home has nothing telling it this image is affected."
- Category: Minor — "a sweep that rotates the step-ca root or readdresses tfmirror.home has nothing telling it this image is affected"
- Note: two halves at different weights. The first is a stale contents list — nit-pick-shaped, a wording correction in D31 and design.md. The second is a missing consumer pointer, and its stated consequence is a future cert rotation or readdressing silently breaking the hook. The more severe half sets the label; rule it Nit pick if you read the second half as the same doc fix.
- Ruling: Is this a doc change?
- Research verdict (answering the ruling): **yes, Markdown only — and half of it is already done.** Half (a) is obsolete: commit `97b5313` ("007 P11") rewrote D31 on 2026-08-14, two days before this triage. D31 now spans `argo-cd/decisions.md:240-257` and names `librados2`/`librbd1`, the `TF_CLI_CONFIG_FILE` Terraform CLI config and the step-ca root explicitly — item-for-item matching `/work/ArgoCDTools/Dockerfile`. `argo-cd/design.md:88-93` was rewritten in the same commit and its residual "nothing general-purpose (D31)" is now accurate. Half (b) stands and is purely additive prose. No Dockerfile instruction, chart, Job template or Python change is implied.
- Research verdict (half b is wider than the card): there is no consumer inventory, only a thin precedent — `AnsibleSpecs/decisions.md:157-171` ("Root rotation mechanism") names two out-of-repo copies of `homelab-root.crt` by path. There are **four** byte-identical copies (md5 `aa4e1a5c…`): the two HelmCharts ones it lists, plus `/work/ArgoCDTools/image/` and `/work/DockerImages/kube-coder-dev-base/`. Same shape for `tfmirror.home`: `Ansible/docs/runbooks/operator-workstation.md:90` names two consumers of `terraform.rc`, and there are four identical copies (md5 `f2af2394…`) — `ArgoCDTools/image/`, `DockerImages/kube-coder-dev-base/`, `DockerImages/modern-app-dev/`, `Ansible/support/iac-image/`. `decisions.md:166` promises `docs/runbooks/step-ca-root-rotation.md`; that file does not exist (`:169` lists writing it as a TODO).
- Research verdict (a stale echo the card did not name): `/work/ArgoCDTools/Dockerfile:1-5` still carries the pre-P11 four-item list in its header comment, contradicted by the file's own body 25 lines below. A comment, not an instruction.
- Ruling (round 2): "Agreed." — recorded, but it does not resolve the narrow-versus-wide question below; re-asked.
- Category (re-derived on the verdict): Nit pick — half (a) is already fixed, so what remains is additive documentation of who holds copies of the step-ca root and `terraform.rc`.
- Question: the remaining half is bigger than #618 described — two missing consumers each for the step-ca root and `tfmirror.home`, plus the unwritten rotation runbook `decisions.md` already promises. Keep it as the card's narrow "point at the hook image", or widen it to "complete both consumer lists and write the promised runbook"?

**Card text:** D31 (:241-243) says the image carries "Terraform, terraform-backend-git, git, and the presync scripts — nothing else". librados2/librbd1, image/terraform.rc and image/homelab-root.crt all ride beyond that, and design.md's image section repeats the same "nothing else (D31)" clause.

Pre-existing; slice 007's P7 reviewer flagged it as outside that phase's mandate.

Second half of the same gap: nothing in the set points at the hook image as a consumer, so a sweep that rotates the step-ca root or readdresses tfmirror.home has nothing telling it this image is affected.

---

## Borderline — Minor / Corner case

### #624a — Charts' render-consumer.sh latches on the render's first `args:` — https://trello.com/c/Utiy8yX7

- Source: card #624, first bullet
- Ask: "Charts' `tests/render-consumer.sh` locates the `args:` block with an awk latch on the render's first `args:`, with nothing tying it to the hook Job (P10 r1 F1). A future fixture manifest that helm sorts ahead of the Job and carries its own `args:` makes the gate fail claiming the hook renders the wrong arguments — a misleading false red, never a false green."
- Category: Minor/Corner case — borderline — "A future fixture manifest that helm sorts ahead of the Job and carries its own `args:` makes the gate fail claiming the hook renders the wrong arguments — a misleading false red, never a false green."
- Note: borderline because the trigger does not exist today — it takes someone adding a fixture manifest that sorts ahead of the Job — and the source bounds the blast radius to a false red. Corner case if you weigh the unreachability; Minor if you weigh that the trigger is an ordinary future edit rather than something nonsensical.
- Ruling: Close.

**Card text (the card's first bullet; the card as a whole reads):** Two mechanical leftovers from slice 007, neither behavioural:

- Charts' `tests/render-consumer.sh` locates the `args:` block with an awk latch on the render's first `args:`, with nothing tying it to the hook Job (P10 r1 F1). A future fixture manifest that helm sorts ahead of the Job and carries its own `args:` makes the gate fail claiming the hook renders the wrong arguments — a misleading false red, never a false green.
- plan.md's P5 done-record cites `tests/consumer/values.yaml:37-40` and `tests/render-consumer.sh:104-106`; P10's later insertion shifted these to `:39-44` and `:124-128`. Content and semantics are correct — only the stamped pointers are stale, and verification.json cites the current lines.

---

## Corner case

### #622 — presync exports an empty stage or namespace argument as an empty TF_VAR_* — https://trello.com/c/lN7XPBXd

- Source: card #622
- Ask: "presync passes an empty `stage` or `namespace` through verbatim, reproducing the silent misnaming that P9 closed."
- Category: Corner case — "Unreachable from a rendered Job — the library chart's Helm `required` guard rejects the empty string too — so today the chart's guard is the whole of the protection."
- Ruling: Close.

**Card text:** Slice 007 P9 r1 F1. presync passes an empty `stage` or `namespace` through verbatim, reproducing the silent misnaming that P9 closed.

Unreachable from a rendered Job — the library chart's Helm `required` guard rejects the empty string too — so today the chart's guard is the whole of the protection. Defence in depth would be a check in presync itself.

---

## Nit pick

### #616 — docs/live-infra-access.md: the mounted kubeconfigs have no cluster-scoped rights at all, not just on nodes — https://trello.com/c/KgHxsx5S — user-visible

- Source: card #616
- Ask: "The doc states this limit only for the `nodes` resource, which reads as a narrow exception." / "Generalise the sentence so the next slice plans against the real limit."
- Category: Nit pick — "Generalise the sentence so the next slice plans against the real limit."
- Note: sub-split is mine — the artefact is operator-and-agent-facing repo documentation rather than a code comment. The wording already cost slice 007 a planned proof path, so it is an impactful one.
- Ruling: I get many complaints from LLMs on this. By now I do feel this should be fixed. Keep.

**Card text:** `~/.kube/config-prd-write` is `kubecoder-rw`, a namespaced-edit identity with no cluster-scoped verb whatsoever — it cannot get/list/patch PVs, nor create namespaces, on prd.

The doc states this limit only for the `nodes` resource, which reads as a narrow exception. Slice 007 planned its PV reattach proof around that write path and found it could not do the job; the fixture's cluster-scoped objects ended up created over SSH (`sudo microk8s kubectl` on srvk8s1) instead.

Generalise the sentence so the next slice plans against the real limit.

### #619 — The argo-cd set names a credential inventory it gives no way to find — https://trello.com/c/wTHP8RE6 — user-visible

- Source: card #619
- Ask: "phases.md A.2/A.4 both owe and read \"the inventory of a run's whole environment\". That inventory exists only at `slices/completed/007_argocd_tools_presync_hook/attachments/credential-inventory.md` and is cited nowhere under `argo-cd/`."
- Category: Nit pick — "Either cite it from the set or promote it into the set"
- Note: the fix is a citation, but the stake is timing — the card's own reason is that "slice 009 plans from the documents", so it wants to land before 009 is planned rather than whenever.
- Ruling: Keep.

**Card text:** phases.md A.2/A.4 both owe and read "the inventory of a run's whole environment". That inventory exists only at `slices/completed/007_argocd_tools_presync_hook/attachments/credential-inventory.md` and is cited nowhere under `argo-cd/`.

Either cite it from the set or promote it into the set — slice 009 plans from the documents, not from 007's plan.md.

### #620 — presync: an unparseable ServiceAccount ca.crt escapes the named-error handler — https://trello.com/c/lAShNeBe — user-visible

- Source: card #620
- Ask: "In ArgoCDTools, a present-but-unparseable `ca.crt` raises `ssl.SSLError` out of `Api`'s TLS-context construction, escaping `__main__`'s `PresyncError` handler."
- Category: Nit pick — "The run still exits non-zero, so the sync stays gated — but the operator gets a traceback instead of a named cause."
- Ruling: Close.

**Card text:** In ArgoCDTools, a present-but-unparseable `ca.crt` raises `ssl.SSLError` out of `Api`'s TLS-context construction, escaping `__main__`'s `PresyncError` handler.

The run still exits non-zero, so the sync stays gated — but the operator gets a traceback instead of a named cause.

Found as slice 007 P3 r1 F3; the completion consult judged it too small to justify a phase.

### #583 — Slice 013 residuals: managed-vm versions.tf still claims the homelab provider is baked into the image — https://trello.com/c/epj7i46y — internal

- Source: card #583
- Ask: "terraform/modules/managed-vm/versions.tf:9-11 says the homelab provider binary is \"baked into the modern-app-dev image, which is the version source of truth\" and cites slices/completed/embed-homelab-provider.md." / "One-line fix: correct the comment and point it at slices/completed/tf-provider-registry.md."
- Category: Nit pick — "One-line fix: correct the comment and point it at slices/completed/tf-provider-registry.md."
- Ruling: Keep.

**Card text:** Doc debt from tf-provider-registry, surfaced three times during slice 013 (PC r1, PC r2, doc phase) — verified false, still open.

terraform/modules/managed-vm/versions.tf:9-11 says the homelab provider binary is "baked into the modern-app-dev image, which is the version source of truth" and cites slices/completed/embed-homelab-provider.md. Neither support/iac-image/Dockerfile nor DockerImages/modern-app-dev/Dockerfile bakes a provider or a filesystem mirror; both resolve registry.terraform.io/pvginkel/* from the tfmirror.home network mirror via /etc/terraform.rc.

One-line fix: correct the comment and point it at slices/completed/tf-provider-registry.md.

The twin claim in docs/runbooks/operator-workstation.md was fixed by 013's doc phase; this one is a code comment in a Terraform module, outside that diff's work list.

### #624b — Slice 007 plan.md's P5 done-record cites stale line numbers — https://trello.com/c/Utiy8yX7 — internal

- Source: card #624, second bullet
- Ask: "plan.md's P5 done-record cites `tests/consumer/values.yaml:37-40` and `tests/render-consumer.sh:104-106`; P10's later insertion shifted these to `:39-44` and `:124-128`."
- Category: Nit pick — "Content and semantics are correct — only the stamped pointers are stale, and verification.json cites the current lines."
- Note: a done-record is a historical stamp of what was true when the phase closed, so "correct it" is not the only defensible answer — leaving it and relying on verification.json's current lines is the other.
- Ruling: Close.

**Card text (the card's second bullet; the card as a whole reads):** Two mechanical leftovers from slice 007, neither behavioural:

- Charts' `tests/render-consumer.sh` locates the `args:` block with an awk latch on the render's first `args:`, with nothing tying it to the hook Job (P10 r1 F1). A future fixture manifest that helm sorts ahead of the Job and carries its own `args:` makes the gate fail claiming the hook renders the wrong arguments — a misleading false red, never a false green.
- plan.md's P5 done-record cites `tests/consumer/values.yaml:37-40` and `tests/render-consumer.sh:104-106`; P10's later insertion shifted these to `:39-44` and `:124-128`. Content and semantics are correct — only the stamped pointers are stale, and verification.json cites the current lines.

---

## Improvement

### #574 — Auto-derive the deterministic MAC instead of writing it out per NIC — https://trello.com/c/PVz7YZAX

- Source: card #574
- Ask: "Every NIC still carries a hand-written `mac_address` that follows a fixed convention (decisions.md \"MAC addressing for managed VMs\": `02:A7:F3:VV:VV:EE` — prefix, VMID as two big-endian bytes, NIC index). It should be computed, not typed."
- Category: Improvement — "It should be computed, not typed."
- Note: the card carries an unresolved design question it calls "the whole point of the card" — where the derivation belongs now that the host_vars are the SoT — which the planner must bottom out before designing.
- Ruling: Close. Nothing is broken.

**Card text:** Every NIC still carries a hand-written `mac_address` that follows a fixed convention (decisions.md "MAC addressing for managed VMs": `02:A7:F3:VV:VV:EE` — prefix, VMID as two big-endian bytes, NIC index). It should be computed, not typed.

Open design question, and it is the whole point of the card: the MACs now live in `ansible/inventories/prd/host_vars/<name>.yml`, which is the single source of truth since the network-devices-host-vars-sot slice, and `terraform/prd/vms.tf` yamldecodes them back out. Ansible also matches netplan interfaces by that MAC. So deriving it inside the `managed-vm` Terraform module — the original plan, written when the MACs lived in vms.tf — would put the derivation on the wrong side of the SoT. Decide where it belongs before designing.

Legacy adoption MACs (`BC:24:11:...` on srvceph1/2/3) must stay explicit until the Ceph rebuilds rotate them.

`terraform/scratch/main.tf` already derives inline for its single-NIC VMs; folding scratch in is opportunistic, not required.

Supersedes slice 002 (retired). Prior material, incl. the module sketch and the bpg case-normalisation caveat: AnsibleSpecs/change_requests/managed_vm_mac_derivation/

### #617 — Slice 009: the tf-presync ServiceAccount needs PV get/list/patch cluster-wide — https://trello.com/c/hARxhMJR

- Source: card #617
- Ask: "a reattach run without it gets a 403, and the hook fails the sync on it."
- Category: Improvement — "Input for 009's A.4 RBAC."
- Note: the rubric has no class for "requirement input to a pending slice", which is what this is — nothing is broken today because 009 has not shipped. Its disposition is to fold into slice 009's `slice.md` rather than become a slice of its own; if 009 shipped without it, the same text describes an outage of the deploy path.
- Ruling: Slice 009 has been planned. Does it need to be updated?
- Research verdict (answering the ruling): **no — 009 already carries it.** The slice is planned and promoted to `slices/009_argocd_standup/` (`plan.md` + `verification.json` present; `backlog/` now holds only 010, 011, 012, 014). Requirement R3 at `plan.md:18-22` quotes phases.md verbatim including "the `tf-presync` ServiceAccount and its RBAC (PV get/list/patch)"; `verification.json:67` (V08) restates it as an acceptance criterion; phase P4 (`plan.md:412-446`, target `../ArgoCDDeploy`) builds it. Source wording is `argo-cd/phases.md:78-82` and `argo-cd/design.md:418`. Nothing ships it today — the library chart's `_tf-presync-hook.tpl` renders only the Job, and ArgoCDTools ships no manifests — which is exactly what P4 is for.
- Research verdict (one wrinkle, already known to 009): `slices/009_argocd_standup/close-out.md:168-183` finding S4 — disposition still blank — records that the hook only ever issues a collection `GET /api/v1/persistentvolumes` and `PATCH` on named ones, so the `get` verb is granted for nothing. `ArgoCDTools/README.md:90` agrees ("needs `list` … and `patch`"). 009 chose to grant the requirement as written rather than narrow it on its own authority.
- Category (re-derived on the verdict): Invalid — the card's ask is already specified in the planned slice it was written for.
- Question: close it as superseded by 009's R3/V08? The unresolved bit is not this card but 009's own close-out finding S4 (the surplus `get` verb), which is a close-out disposition rather than a triage item.
- Ruling (round 2): "Close then." — closes as superseded by slice 009's R3 / V08. Final category: Invalid.

**Card text:** Verified behaviour from slice 007, not speculation: a reattach run without it gets a 403, and the hook fails the sync on it.

Narrowing the grant with `resourceNames` was considered and rejected — 007's throwaway proof SA could enumerate its two fixture volumes, but the real hook cannot know volume names in advance.

Input for 009's A.4 RBAC.

### #635 — Retire the now-unused HA_URL / HA_TOKEN from the IaC agent — https://trello.com/c/ZpUz4EYx

- Source: card #635
- Ask: "It was the only consumer of HA_URL and HA_TOKEN, which are still wired up." / "Left out of that commit because retiring them touches four places plus a live host"
- Category: Improvement — "It was the only consumer of HA_URL and HA_TOKEN, which are still wired up."
- Note: the card states an ordering constraint with a live consequence — "dropping the grant while a `!bao` ref survives hard-fails iac-impl at next container start" — so the sequencing is part of the ask, not an implementation detail.
- Ruling: Agreed.

**Card text:** send_message.py is gone (Ansible 7cdc788): the IaC pipelines now report through jenkins-telegram-bot and raise the rest via JenkinsPipelineUtils' notify var. It was the only consumer of HA_URL and HA_TOKEN, which are still wired up.

Left out of that commit because retiring them touches four places plus a live host:

- `support/iac-agent/etc/iac/secrets.example.yaml` — the `HA_URL` literal and the `HA_TOKEN` `!bao kv/iac/homeassistant#token` ref
- `ansible/inventories/prd/group_vars/openbao.yml` — the `kv/iac/homeassistant` grant in `openbao_iac_agent_kv_paths`
- `docs/runbooks/iac-cold-boot.md` — its `HA_TOKEN` step
- the OpenBao leaf itself, and srviac's live `/etc/iac/secrets.yaml`

Order matters: dropping the grant while a `!bao` ref survives hard-fails iac-impl at next container start. Remove the refs and converge first, then the grant and the leaf.

The token is minted distinct from the homeassistant-mcp chart's (decisions.md, per-consumer named accounts), so nothing else loses access.

---

## Feature

### #457 — Project the Terraform provider credentials into KubeCoder envs so plan/apply work there — https://trello.com/c/tZXLaa6o

- Source: card #457
- Ask: "Anything that contacts Proxmox still fails, and not because of the backend — the **provider credentials are not present in the environment**." / "Seed the credential values into the OpenBao catalog leaves ... Project them in `.kubecoder/config.yaml` under `secrets:` as `TF_VAR_*` names"
- Question: the card's own, and it can kill or halve the card — do we want prd apply capability inside a KubeCoder env at all, given `CLAUDE.md` codifies that the operator runs all terraform? The narrower option it names is scratch-only credentials.
- Category: Feature — "Seed the credential values into the OpenBao catalog leaves (`eso/prd/kubecoder/{dev,prd}/catalog`) so they appear in `kc catalog`."
- Note: the stakes are doctrinal, not technical — the card itself says "Putting live Proxmox root credentials into an environment where an agent operates is a meaningful change to that posture."
- Question: "agreed" read as the card as written — project the real Proxmox credentials, with the prd-versus-scratch scope call made inside the slice, since the card carries it as its own step 3. Confirm that, or scope it to scratch-only from the outset? It is the one item in this batch that moves the "operator runs all terraform" posture, so I would rather have it explicit than assumed.
- Ruling: Agreed.
- Ruling (round 2): "I think I misunderstood what the card was about. The HelmCharts/scripts/setup-env.sh script has been made to work. That pulls in those secrets without making them available always. Does that close out this card?"
- Research verdict (answering the ruling): **no — different credentials, different Terraform root, different names.** `HelmCharts/scripts/setup-env.sh` is a source-me script for the HelmCharts deploy CLI (`HelmCharts/CLAUDE.md:32,56`). It exports `HOMELAB_CEPH_USER`/`_KEY`, `HOMELAB_S3_ADMIN_*`, `HOMELAB_IAC_PROVISIONER_TOKEN` from `kv/shared/<cluster>/*` and `kv/eso/<cluster>/*` (lines 53-59), plus `TF_VAR_postgres_admin_password` (line 84) and `HOMELAB_BACKUP_SERVER_TOKEN` (line 98). Against this card's five: `proxmox_endpoint`, `proxmox_username`, `proxmox_password` and `dns_reservation_token` are **not fetched at all**; `backup_server_token` is the same value under the wrong name. The Ansible roots' credentials live in a different subtree entirely — `kv/iac/proxmox`, `kv/iac/dns-reservation`, `kv/iac/backup-server` (`support/iac-agent/etc/iac/secrets.example.yaml:150-158`) — which the script never touches. The "made to work" commit is `9b065aa` (2026-08-13), a four-line change swapping `bao …`/`kubectl …` for `cexec iac …`; it added no variables.
- Research verdict (what still fails): `terraform plan` against `terraform/prd` or `terraform/scratch` still errors on required variables with no defaults, before the provider's `HOMELAB_*` env fallback is ever consulted — `terraform/prd/providers.tf:1-45` passes them as explicit root-module variables. Missing for prd: `TF_VAR_proxmox_endpoint`, `TF_VAR_proxmox_password`, `TF_VAR_dns_reservation_url`, `TF_VAR_dns_reservation_token`, `TF_VAR_backup_server_url`, `TF_VAR_backup_server_token` (`proxmox_username` and `proxmox_insecure` have defaults). For scratch: the first four. Separately, `terraform/prd/providers.tf:21-24` sets `ssh { agent = true }` and nothing in the environment starts an agent — `kubecoder-keys.sh:83` writes `SSH_KEY_PVE` to disk but loads no agent.
- Research verdict (why it still matters): the script is nonetheless the answer to this card's doctrine question. It is on-demand — one `bao kv get` per leaf at source time, `export`ed into that one shell, nothing written to disk, nothing in `.kubecoder/config.yaml` `secrets:`, nothing surviving the shell, and it needs a live `BAO_TOKEN` first. That is materially safer than the card's proposed route (catalog leaf → persistent pod env), and it is mechanically portable: `cexec` mirrors the dev shell's env into the sidecar, so exports made this way do reach `cexec iac terraform`.
- Question: this changes the shape of the ask rather than closing it — an Ansible-side equivalent of `setup-env.sh` reading `kv/iac/*` and exporting the `TF_VAR_*` names the roots actually take, on demand, instead of seeding the catalog and projecting into the pod env. Rewrite the card to that, and drop its step-1/step-2 route? Doing so also answers its own "worth deciding before starting" — the credentials never become always-available, so the posture question narrows to which roots the operator sources for.

**Card text:** Follow-on from #446 (slice 135, `terraform-backend-git`), which is now fully discharged.

#### Where things stand

The state backend is reachable from a KubeCoder env. `pvginkel/Ansible`'s `.kubecoder/config.yaml` runs the `terraform-backend-git` sidecar (commit `f13ca0e`), and from the `iac` sidecar `terraform init` and state reads succeed against both `terraform/prd` and `terraform/scratch` — verified live: 38 real prd resources, 8 scratch. The backend URL names the git store (`pvginkel/TerraformState`, ref `main`), so this daemon and the one `iac-impl` runs on **srviac** resolve to the same state. Reads here are the real thing.

#### The gap

Anything that contacts Proxmox still fails, and not because of the backend — the **provider credentials are not present in the environment**.

`terraform/prd` takes them as variables (`terraform/prd/variables.tf`):

- `proxmox_endpoint`
- `proxmox_username`
- `proxmox_password`
- `dns_reservation_token`
- `backup_server_token`

(plus `proxmox_insecure`, `dns_reservation_url`, `backup_server_url` — non-secret.)

Only `terraform.tfvars.example` is checked in. Verified absent in the pod: no `TF_VAR_*` or `PROXMOX_*` env vars in the `iac` sidecar, and `kc catalog` carries none of these secret names on **either** the dev or prd KubeCoder instance. So `plan`/`apply` fail on missing variables.

#### What the work is

Cross-repo, Ansible-led:

1. Seed the credential values into the OpenBao catalog leaves (`eso/prd/kubecoder/{dev,prd}/catalog`) so they appear in `kc catalog`.
2. Project them in `.kubecoder/config.yaml` under `secrets:` as `TF_VAR_*` names, so the `iac` sidecar picks them up. Note the secret-mapping change is restart-forcing — needs `kc env sync --mode full-sync`.
3. Re-verify with a real `terraform plan` against `terraform/scratch` first (lower stakes than prd), then decide whether prd plan/apply should be permitted from a dev environment at all.

#### Worth deciding before starting

**Do we actually want prd apply capability inside a KubeCoder env?** Today the split is deliberate: real terraform runs happen in the `IaC/*` Jenkins pipelines or by hand on srviac, and `CLAUDE.md` codifies "the operator runs all terraform." Putting live Proxmox root credentials into an environment where an agent operates is a meaningful change to that posture. A narrower option is scratch-only credentials, which unlocks iteration on the disposable fleet without handing over prd.

`CLAUDE.md` was updated in `f455a18` to describe the current half-state accurately; it needs revisiting again if this lands.

### #575 — Keycloak OIDC login for Grafana and pgAdmin — https://trello.com/c/LzTmUT83

- Source: card #575
- Ask: "Put Grafana and pgAdmin behind the homelab Keycloak (`homelab` realm at https://auth.ginbov.nl/realms/homelab) instead of their app-local logins, same shape as dnsmasq / electronics-inventory / iot / guacamole / open-webui / zigbee2mqtt already use"
- Category: Feature — "Put Grafana and pgAdmin behind the homelab Keycloak ... instead of their app-local logins"
- Note: the card names its open questions as refinement work (group-to-role mapping, keeping local admins as fallback), so they belong to planning rather than here. It also carries a forward constraint: a future keycloak-tf slice must import these hand-made clients, never recreate them.
- Ruling: Agreed.

**Card text:** Put Grafana and pgAdmin behind the homelab Keycloak (`homelab` realm at https://auth.ginbov.nl/realms/homelab) instead of their app-local logins, same shape as dnsmasq / electronics-inventory / iot / guacamole / open-webui / zigbee2mqtt already use: per-app confidential client, secret in OpenBao at `eso/<cluster>/<app>/<stage>/oidc`, materialised by ESO, OIDC config in the chart/release values.

These two are the clean chart-side wins. Grafana has native `auth.generic_oauth` (upstream chart, so the ExternalSecret goes through the release's manifests.yaml); pgAdmin needs a `config_local.py` in `charts/pgadmin/files/` with the client secret injected as an env var.

Clients are hand-created in the realm until keycloak-tf lands — that slice should then import these, never recreate. Redirect URIs are the internal `.home` hostnames even though the IdP is public.

Open question for refinement: per app, map a Keycloak group to an elevated role (Grafana Admin, pgAdmin admin) or keep everyone at base and manage in-app? And do we keep each app's local admin as fallback once OIDC is verified?

Was part of slice 004 (retired). Prior material: AnsibleSpecs/change_requests/oidc_app_rollout/

### #125 — Telegram IaC bot — messaging hub, dead-man's switch, report rendering — https://trello.com/c/0XOqhECV

- Source: card #125
- Ask: "New container, dedicated namespace: in-cluster message API (TF-minted creds), text/image/render-URL messages, dead-man's switch funneling to healthchecks.io (only external leg), scheduled reports, possible Alertmanager channel. Replaces send_message.py."
- Category: Feature — "New container, dedicated namespace: in-cluster message API (TF-minted creds), text/image/render-URL messages, dead-man's switch funneling to healthchecks.io (only external leg), scheduled reports, possible Alertmanager channel."
- Research: which of this card's scope is already shipped? A Telegram bot now carries the IaC pipeline reports, so the card may be wholly or partly superseded — establish specifically what exists of the in-cluster message API, the healthchecks.io dead-man's switch, scheduled reports and the Alertmanager channel, and what remains unbuilt.
- Research verdict: **not superseded — the card stands in full.** What shipped in `123f0f2` is only the retirement half: the IaC pipelines dropped `send_message.py` (both copies deleted) and now ride the pre-existing `jenkins-telegram-bot`, which is a different component — a Deployment inside the jenkins chart, in the `jenkins` namespace, SSE-pull from Jenkins, text-only, one hardcoded chat, a single static ESO secret. Its own architecture file says "It exposes no inbound service of its own." The card's change request agrees the Jenkins-chart bot "is a different thing… not v1 scope". Every item of the card's actual scope is NOT BUILT: dedicated namespace, in-cluster message API, TF-minted creds (no telegram resource in HomelabTerraformProvider), image messages, render-URL messages, healthchecks.io dead-man's switch (one repo-wide hit, and it is prose in a plan document), scheduled reports (no cron or scheduler anywhere), Alertmanager receiver. All four queued consumers are unbuilt too, each still only a change request — trivy (#129), update-train (#126), rotation instructions (#128), destroys>0 alerts (#127).
- Research verdict (an overlap worth knowing before this is grouped): `/work/DockerImages/docs/alert-manager/plan.md` — written 2026-08-12, header says not yet implemented — plans the Alertmanager half as a *separate* sixth channel using Alertmanager's native `telegram_configs` plus an SMTP gateway, explicitly not routed through the Jenkins bot. That document, this card's Alertmanager clause, your comment on it, and #625's "no receiver configured" half are all the same missing delivery path.
- Ruling: Agreed.

**Card text:** Bundle at AnsibleSpecs/change_requests/telegram_iac_bot/. New container, dedicated namespace: in-cluster message API (TF-minted creds), text/image/render-URL messages, dead-man's switch funneling to healthchecks.io (only external leg), scheduled reports, possible Alertmanager channel. Replaces send_message.py. Consumers queued: trivy reports, update-train reports, rotation instructions, destroys>0 plan alerts. Run /write-slice when ready.

**Comment — Pieter van Ginkel, 2026-08-07T20:12:35.420Z:** This also needs to be integrated with Prometheus Alertmanager.

---

## Test gap

### #129 — CI quality gates + trivy — lint/validate/test gates on the push-to-prod pipelines — https://trello.com/c/QaU5mlMx

- Source: card #129
- Ask: "ansible-lint strict (+fix 6 findings) + yamllint + syntax-check; terraform fmt/validate (fmt fails today); go test/vet before provider publish (currently untested binaries ship); helm lint + kubeconform + values.schema.json reference chart; trivy warn-only in DockerImages."
- Category: Test gap — "go test/vet before provider publish (currently untested binaries ship)"
- Note: the card records one observed failure inside the gap — "terraform fmt/validate (fmt fails today)" — so adding the gate turns something already red. It also spans four repos with each gate independent, which is a natural split point if the group gets too big.
- Ruling: Agreed.

**Card text:** Bundle at AnsibleSpecs/change_requests/ci_quality_gates/. ansible-lint strict (+fix 6 findings) + yamllint + syntax-check; terraform fmt/validate (fmt fails today); go test/vet before provider publish (currently untested binaries ship); helm lint + kubeconform + values.schema.json reference chart; trivy warn-only in DockerImages. Four repos, each gate independent. Run /write-slice when ready.

---

## Decision

### #576 — Decide: OIDC on the microk8s apiserver (gates Headlamp SSO) — https://trello.com/c/P7y8oouI

- Source: card #576
- Ask: "So the question is whether we want OIDC on the prd apiserver at all. If yes: it is an Ansible/microk8s change plus RBAC, and Headlamp SSO follows from it. If no: Headlamp stays on its current static admin-user ServiceAccount token pasted into the login form, and we close the idea out."
- Question: do we want OIDC on the prd microk8s apiserver? Your answer is the disposition — yes routes it to a slice, no closes both this and the Headlamp SSO idea. Either way the card says the answer belongs in `decisions.md`.
- Category: Decision — "A doctrine call before any Headlamp work, not a chart change."
- Ruling: I really would like OIDC on Headlamp, so that means it's needed yes.
- Category (re-derived on the answer): Feature — "If yes: it is an Ansible/microk8s change plus RBAC, and Headlamp SSO follows from it." The ruling settles the doctrine question, so the item stops being a Decision and becomes the work it gates: the apiserver `--oidc-*` flags, RBAC binding the mapped Keycloak group, a `decisions.md` entry, and Headlamp SSO on top.
- Ruling (round 2): "Yes." — the re-label to Feature is confirmed. The card moves out of the Decision group and carries the operator's ruling as a settled requirement.

**Card text:** A doctrine call before any Headlamp work, not a chart change.

Headlamp can be pointed at Keycloak through its chart values, but it forwards the id_token straight to the kube-apiserver — so it only actually authorises if microk8s' apiserver trusts the issuer (`--oidc-issuer-url=https://auth.ginbov.nl/realms/homelab`, `--oidc-client-id=headlamp`, a username/groups claim) and RBAC binds the mapped Keycloak group. Setting the chart flags alone does nothing useful.

So the question is whether we want OIDC on the prd apiserver at all. If yes: it is an Ansible/microk8s change plus RBAC, and Headlamp SSO follows from it. If no: Headlamp stays on its current static admin-user ServiceAccount token pasted into the login form, and we close the idea out.

Either way the answer belongs in decisions.md — it affects more than Headlamp.

Was part of slice 004 (retired). Prior material: AnsibleSpecs/change_requests/oidc_app_rollout/

### #623 — Ruling wanted: does argo-cd/phases.md carry a shipped marker? — https://trello.com/c/fDkBy887

- Source: card #623
- Ask: "Every item in A.1 (shipped as slice 006) and A.2 (shipped as slice 007) still reads `- [ ]`, so the set read on its own presents shipped work as pending."
- Question: should `phases.md` carry a shipped marker at all, given status already lives in `slices/README.md` and on the board? If yes, the card notes the ruling must also cover items that are code-complete but owed to an operator keystroke.
- Category: Decision — "Ruling wanted: does argo-cd/phases.md carry a shipped marker?"
- Ruling: Close as nit. The board tracks status. Just leave it. Maybe at some point I'll ask the checks to be updated so we're clear on what we're missing still.
- Category (operator override): Nit pick — recorded as the operator's label, not re-derived.

**Card text:** Every item in A.1 (shipped as slice 006) and A.2 (shipped as slice 007) still reads `- [ ]`, so the set read on its own presents shipped work as pending.

007's doc phase did not invent a convention: no `[x]` exists anywhere in the file, and status is tracked in slices/README.md and on the board.

If phases.md should carry one, the ruling also needs to say how it handles items like A.2's "CI publishes registry:5000/argocd-hook:<n>" — code-complete but owed to an operator keystroke.

### #69 — site.yml layout — does it still make sense? — https://trello.com/c/9xe2i4qB

- Source: card #69
- Ask: "Is site.yml still the right organizing artefact for \"apply all roles\"? Analysis request, no direction proposed yet."
- Question: is this still live? It has been parked since it was filed, names no direction, and its stated relation is "Soft-related to the IaCAgent friction" — which has since been reworked by slice 013. Worth commissioning the analysis, or closing it?
- Category: Decision — "Analysis request, no direction proposed yet."
- Ruling: Keep as an open question.
- Question: reading this as park, not file — the tracker's deferred disposition (the Later list), since a Decision with no answer yields no slice. Confirm, or leave it in Inbox?

**Card text:** Open design question parked at AnsibleSpecs/change_requests/site_yml_layout/. Is site.yml still the right organizing artefact for "apply all roles"? Analysis request, no direction proposed yet. Soft-related to the IaCAgent friction.

---

## Outside the rubric — operator chore

### #577 — Jenkins OIDC login via oic-auth (in-app, no chart change) — https://trello.com/c/AEO1suJp

- Source: card #577
- Ask: "Operator task in the running Jenkins, filed here so it is not forgotten — move it to Operator Actions if you would rather work it there."
- Question: the card offers the routing choice itself. Reading is Chore → Operator Actions, since every step is hand-work in the running Jenkins and the realm, with no repo change to plan. Confirm, or keep it in the workflow?
- Category: Chore (outside the rubric) — "Operator task in the running Jenkins, filed here so it is not forgotten — move it to Operator Actions if you would rather work it there."
- Note: the card also needs a `jenkins` confidential client and secret created in the realm by hand, same as the other apps — that is operator work too.
- Ruling: Agreed.

**Card text:** Operator task in the running Jenkins, filed here so it is not forgotten — move it to Operator Actions if you would rather work it there.

The Jenkins chart has no JCasC / security-realm surface (no casc or jenkins.yaml anywhere in it), so there is no comfortable chart-side path and we deliberately do not want a half-JCasC config bolted in for this. The natural route is in-app:

1. Install the `oic-auth` plugin.
2. Configure the OIDC security realm against the `homelab` realm (https://auth.ginbov.nl/realms/homelab), client `jenkins`, via the well-known config endpoint.
3. Keep an in-process emergency admin until the realm is verified.

Needs a `jenkins` confidential client in the realm and its secret, same convention as the other apps.

If we ever add a proper JCasC surface to the chart, fold the realm in then — separate decision.

Was part of slice 004 (retired). Prior material: AnsibleSpecs/change_requests/oidc_app_rollout/
