# Triage — 2026-08-30

Scope: **card 737 only**, at the operator's request. The rest of the Ansible-tagged
intake queue is untouched by this run.

Headings inside `**Card text:**` blocks are demoted so a source's own `##` does not
collide with this outline. Said once, here.

**Ruling vocabulary** — write on the `Ruling:` line, in your own words.

Label rulings: `answer: …` (a new fact; the label is re-derived) · `override: <category>`
(your label, recorded as yours, not re-derived) · `remark: …` (recorded verbatim; moves
nothing unless it contains a fact).

Dispositions: `close` (binding — the item dies) · `later` (parked) · `agreed` (the card
as written plus any recommendation here, to the normal route) · `apply the suggested
edit` (a ceiling — the literal named change and nothing beyond) · `conditional: <ruling>
if <fact>` (the fact becomes a research line and the item comes back) · `split: …` ·
`superseded by …`.

---

## Major

### 737 — internal_tls leaf certs have no scheduled renewal — https://trello.com/c/ZPwdycpy

- Source: Trello Triage card #737
- Ask: "Nothing renews the internal_tls step-ca leaf certificates on a schedule. […] The internal_tls leaves are converged only by site.yml under the manual iac-apply. […] The PVE leaves (notAfter Sep 10 2026) crossed the 14-day renewal threshold on 27 Aug […] drift is --check-only and structurally cannot sign, so the renewal itself still needs a hand. Likely shape: a playbook plus a stage in Jenkinsfile.iac-scheduled-certs, mirroring the weekly-run / 14-day-window logic already used for the SSH host certs. Needs a call on which hosts and groups it covers."
- Question: the card states one — "Needs a call on which hosts and groups it covers." Session research narrows it to a concrete set: 10 leaves on 10 hosts (pve/pve1/pve2, srvk8s1/2/3, srvk8sdev, srvvault1/2/3), reached through three playbooks (`site.yml`, `site-k8s.yml` at two `--limit`s, `site-openbao.yml`). Is the answer "all ten", or do you want the k8s leaves held back — see the Note?
- Category: Major — "The PVE leaves (notAfter Sep 10 2026) crossed the 14-day renewal threshold on 27 Aug, and the daily drift job failed on a latent check-mode bug instead of reporting it."
- Note: two things the card does not surface, both bearing on what you are ruling. **(1)** The card names three consumer roles; concretely that is **10 leaves across 10 hosts and three convergence paths**, not the one `site.yml` path the card describes — `site.yml` covers only the three pveproxy leaves. `srvk8s4` drops out (`microk8s_worker_only: true`), so it is 3 of the 4 prd k8s nodes. **(2)** Unlike the SSH-cert job this would mirror, renewal here **notifies service restarts**: `Restart microk8s kubelite` on prd control-plane nodes, plus `Reload pveproxy` and `Reload openbao`. A weekly job that can bounce the prd control plane is a different risk shape from `renew-host-certs.yml`, which notifies nothing. The design call is the planner's; whether you accept a scheduled kubelite bounce at all is yours.
- Research: — (settled in-session; recorded on the card as a dated triage-research comment)
- Ruling: **agreed** — "Yes, this is fine. I'll start the slice today so there's no need to rotate the certs now." The hosts/groups question was **not** answered and stays open for the planner. The operator declined a manual pre-deadline rotation of the PVE leaves on the grounds that the slice starts today.

**Card text:**

> ### internal_tls leaf certs have no scheduled renewal
>
> Reporter: Jeeves (@jeevesginbov) · Labels: `red` Ansible · no members, no due date, no checklists, no attachments, no comments
>
> Nothing renews the internal_tls step-ca leaf certificates on a schedule. iac-scheduled-certs only runs playbooks/renew-host-certs.yml — SSH host certs. The internal_tls leaves are converged only by site.yml under the manual iac-apply.
>
> Consumers: proxmox_host (/etc/pve/local/pveproxy-ssl.pem on pve, pve1, pve2), microk8s (apiserver homelab cert), openbao.
>
> Surfaced 2026-08-30. The PVE leaves (notAfter Sep 10 2026) crossed the 14-day renewal threshold on 27 Aug, and the daily drift job failed on a latent check-mode bug instead of reporting it. That bug is fixed in 2ec9d0f, so drift now reports a due renewal as changed — visible, but drift is --check-only and structurally cannot sign, so the renewal itself still needs a hand.
>
> Likely shape: a playbook plus a stage in Jenkinsfile.iac-scheduled-certs, mirroring the weekly-run / 14-day-window logic already used for the SSH host certs. Needs a call on which hosts and groups it covers.
