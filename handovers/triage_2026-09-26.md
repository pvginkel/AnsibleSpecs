# Triage 2026-09-26 — status document

Scope: DI-8, ANS-125 and the chat that scoped them, at the operator's request. Raw material,
verbatim: [`triage_2026-09-26_raw.md`](triage_2026-09-26_raw.md). Headings inside a `**Card
text:**` block are demoted by two levels.

**How to rule** — write on an item's `Ruling:` line, in your own words. Forms with fixed meaning:

- Label: `answer: …` (a new fact; the label is re-derived), `override: <category>` (your label, as
  yours), `remark: …` (recorded; moves nothing unless it holds a fact).
- Disposition: `close`, `later`, `agreed` (the item as written plus this document's
  recommendation), `apply the suggested edit` (the literal change and nothing beyond it),
  `conditional: <ruling> if <fact>`, `split: …`, `superseded by …`.

Chat rulings already given are pre-filled, quoted, with what they answered; change any you
disagree with.

## Major

### DI-8 — registry-cleanup deletes images a deploy repo pins; lifting the pause is part of it
- Source: DI-8 — registry-cleanup must never delete an image a deploy repo pins (GC --delete-untagged, keep-newest cap)
- Ask: "registry-cleanup must never delete an image a deploy repo pins (GC --delete-untagged, keep-newest cap)" … "Remove that line, and with it the comment, once cleanup reads the in-use set (deploy-repo pins, running pods and ReplicaSets) and fails closed when it can't read it."
- Question: who lifts the pause? A RegistryDeploy push auto-syncs, so if the slice removes `suspend: true` itself, the first run that deletes anything is the slice's. Should the slice do that, or stop at a clean live dry-run and hand the push to you?
- Category: Major — "After a power cut, Keycloak could not pull its image and took DHCP down with it."
- Note: no standing decision contradicts it. The same design doc's §8 once called "a cluster in-use scan, and any RBAC change" unnecessary; §14 supersedes that for Argo-migrated apps.
- Ruling: (chat, 2026-09-26) "Go" — to the session's proposal: cleanup protects everything the cluster references (running pods, plus the image in every Deployment, ReplicaSet, StatefulSet, DaemonSet and CronJob), deletes nothing if it can't read the cluster, keeps the untagged-image GC but skips it while any image is referenced by a digest no tag points at, reads the cluster and not the deploy repos, then lifts the pause.
- Ruling: (operator, 2026-09-26) "I decided earlier that knowing what's deployed is out of scope. If our continuous rebuilds work, there will always be a fresh image. And I haven't yet enabled auto cleanup, so it's not yet a problem." — the in-use set is dropped; the pause question stays open for the planner. → slice 031

**Card text:**

- Reporter: jeeves
- Created: 2026-09-25
- Updated: 2026-09-25
- State: New · Type: Task · Tags: none
- Parent: EPIC-5 (DHCPOutage)
- Relates: ANS-125, DI-5

##### Description

On 2026-09-25 the nightly GC deleted `keycloak@sha256:fa01e691…`. The GC is `registry garbage-collect --delete-untagged`, hardcoded in `registry-cleanup/app/main.py`. KeycloakDeploy still pinned that digest after #2548 rebuilt the tag. After a power cut, Keycloak could not pull its image and took DHCP down with it.

Two rules delete deployed images:
- **GC `--delete-untagged`** removes a pinned digest as soon as its tag moves.
- **The keep-newest-10 cap ignores deploy pins.** `webhook-relay:2485` (argocd-prd), `dhcpapp-ui:45` and `electronics-inventory-ui:251` were 5–6 builds from deletion.

**The job is paused; lifting the pause is part of this card.** The 2026-09-25 stop-gap sets `suspend: true` in RegistryDeploy `chart/templates/registry-cleanup-cronjob.yaml`. Remove that line, and with it the comment, once cleanup reads the in-use set (deploy-repo pins, running pods and ReplicaSets) and fails closed when it can't read it. ANS-125 (tags vs. digests) settles part of the answer. This is DI-5's open question, now urgent. While paused, the registry only grows; keep an eye on its storage.

##### Comments

None.

## Minor

### C1 — upstream images are pinned by bare digest in the deploy repos
- Source: chat, 2026-09-25 (session's scope question 1)
- Ask: (session's wording) "About 17 references across roughly 13 apps (nginx, jenkins, elastic, plex, gluetun and others) carry a bare digest that the migration froze in place." … "they never update: NginxDeploy's `nginx: :mainline` default is now overridden and does nothing."
- Category: Minor — "they never update: NginxDeploy's `nginx: :mainline` default is now overridden and does nothing."
- Note: not a deletion risk ("those images don't live in our registry"); what it needs is a policy for who bumps outside versions.
- Ruling: (chat, 2026-09-26) "Go" — to "with outside images as a separate card": its own card in ANS intake, not this slice.
- → own card in ANS intake

**Card text:**

> 1. **Outside images are also pinned by digest.** About 17 references across roughly 13 apps (nginx, jenkins, elastic, plex, gluetun and others) carry a bare digest that the migration froze in place. This isn't a deletion risk, because those images don't live in our registry. But they're the same kind of hash you dislike, and they never update: NginxDeploy's `nginx: :mainline` default is now overridden and does nothing. I'd give these their own card, because they need a policy for who bumps outside versions.

### C2 — KubeCoderDeploy runs `kube-coder-tunnel-reclaim:latest`
- Source: chat, 2026-09-25 (session's scope question 2)
- Ask: (session's wording) "**KubeCoderDeploy runs `kube-coder-tunnel-reclaim:latest`,** a moving tag that nothing pins."
- Category: Minor — "a moving tag that nothing pins"
- Ruling: (chat, 2026-09-26) "Go" — to "It's small, so I'd fold it into this slice."
- → slice 031

**Card text:**

> 2. **KubeCoderDeploy runs `kube-coder-tunnel-reclaim:latest`,** a moving tag that nothing pins. It's small, so I'd fold it into this slice.

### C3 — registry-cleanup reads `node-24` and `jdk-21` as build numbers
- Source: chat, 2026-09-26 (session's finding while listing matrix images)
- Ask: (session's wording) "registry-cleanup counts any tag ending in a dash and a number as an old build number. So it treats `node-24` and `jdk-21` as build 24 and build 21, not as named tags."
- Question: the classifier C3 wants changed is the decided one (Collides, below), shared by registry-cleanup and the version-poller. Overrule it, so named tags like `node-24` count as tracking tags?
- Category: Minor — "cleanup could delete them once they're 26 weeks old without a rebuild."
- Collides: DockerImages `docs/registry-management/version-poller-redesign.md` §4 — "The mirror classifier used by both tools: a tag is **versioned** (numbered history) iff it matches `^\d+$` or `^.+-\d+$`; everything else is a **tracking tag** (covers `latest`, `*-latest`, and semver pins like `1.35.5`, `v26.0`, `26.5.1-postgres-health-ispn`)."
- Note: the session's own text also says "They survive only because cleanup always keeps a repo's newest tag" and "They're rebuilt often enough that it hasn't happened", so no image has been lost to it. The per-build matrix tags (ANS-125) change which tag is a repo's newest.
- Ruling: (chat, 2026-09-26) "Go" — to "The per-build tags will need clear rules in cleanup anyway, so I'd put this in the same slice."
- Ruling: (operator, 2026-09-26) "As for what counts as a build number. I feel the now obvious answer is that we just make this explicit using a label. That solves a whole slew of problems. It does need an alternate Kaniko invocation and a cross repo migration." … "Let's go with this then." — superseded by: the image's `org.webathome.poller.tracking-tag` label decides which tag is tracking; the name pattern goes; unlabelled images are never cleaned. → slice 031

**Card text:**

> **Something I noticed while checking:** registry-cleanup counts any tag ending in a dash and a number as an old build number. So it treats `node-24` and `jdk-21` as build 24 and build 21, not as named tags. That means cleanup could delete them once they're 26 weeks old without a rebuild. They survive only because cleanup always keeps a repo's newest tag. They're rebuilt often enough that it hasn't happened. The per-build tags will need clear rules in cleanup anyway, so I'd put this in the same slice.

## Operator chore

### C4 — full analysis of what is in the registry, and deletion rules
- Source: chat, 2026-09-26 (operator)
- Ask: "I assume at some point I need to do a full analysis of what's in the registry and decide what to delete by what rules. Make sure there's an operator actions card for that, if there isn't one already."
- Ruling: (operator) as asked. None existed. → Operator Action card

**Card text:**

> As for the gradual migration: I assume at some point I need to do a full analysis of what's in the registry and decide what to delete by what rules. Make sure there's an operator actions card for that, if there isn't one already.

## Decision

### ANS-125 — reconsider digest pins in the deploy repos
- Source: ANS-125 — Image pins by digest: how the deploy repos came to carry hashes, and the operator's ask to reconsider
- Ask: "I didn't realize DockerImages is writing hashes. I really don't like that. I would like a card explaining how this came to be and an ask by me to reconsider this."
- Question: the per-build matrix tag breaks a standing rule (Collides, below): a matrix build pushes exactly one tag, and `kaniko2` rejects anything else. Overrule the rule for matrix images, so a matrix build may push `<tag>` + `<tag>-<build>`?
- Category: Decision — "an ask by me to reconsider this"
- Collides: DockerImages `docs/registry-management/version-poller-redesign.md` §4 "Tag scheme (enforced by `kaniko2`)" — "Builds push exactly one of: … **one tag** — a version/matrix tag such as `1.35.5` or `26.5.1-postgres-health-ispn`. A single tag is **by definition** the tracking tag. Anything else is an error. **No exotic schemes.**"
- Note: the stakes are an outage, not a preference — "The next keycloak rebuild orphans that digest the same way, and only the paused cleanup keeps it from being deleted." Keycloak's `rebuild-at` is 2026-10-01; the pause holds until DI-8 lands.
- Ruling: (chat, 2026-09-25/26) "Arguably all matrix builds get this behavior." … "Go" — to the session's recommendation: pin tags only, never digests, for images from the estate's registry; every matrix build pushes `<tag>-<build>` next to its named tag and the pin stage writes the per-build tag wherever a `deploy-pins.json` exists (today only keycloak); consumers of the named tags keep floating; the migrated "digest the release runs" comment is corrected in every deploy repo and D53 amended.
- Ruling: (operator, 2026-09-26) the §4 collision is answered by the same label ruling as C3: `kaniko2` takes an explicit tracking tag, so a matrix build may push `<tag>` + `<tag>-<build>`. → slice 031

**Card text:**

- Reporter: jeeves
- Created: 2026-09-25
- Updated: 2026-09-25
- State: New · Type: Task · Tags: none
- Parent: EPIC-5 (DHCPOutage)
- Relates: DI-5, DI-8

##### Description

###### The ask (operator, 2026-09-25)

> I didn't realize DockerImages is writing hashes. I really don't like that. I would like a card explaining how this came to be and an ask by me to reconsider this.

###### How it came to be

DockerImages itself writes tags, not hashes. The hash that broke Keycloak came from HelmCharts, by way of the Argo CD migration:

1. **HelmCharts' deploy CLI has resolved every image to a digest at deploy time since 2024-08.** `tools/chart_tools/resolve_helm_args.py` calls `get_digest` and passes `--set images.<x>=@sha256:…`. A release therefore never ran a tag, so a rebuilt tag could not change a running release underneath it. Every deploy re-resolved, so a stale digest never lasted.
2. **The Argo CD bulk migration (D51, 2026-09-23/24) froze those digests into the deploy repos.** `support/argo-migrate/argo_migrate.py` `compose_values` copied `helm get values` into each `config/<stage>/values.yaml`, so the first Argo render would match the running release. It labelled them "each image at the digest the release runs (argo-cd D53)".
3. **D53 made builds the pin writers, and they write tags.** DockerImages' "Write image pins" stage writes the build number (`:<build>`), and app builds do the same through `cicd.writeVersionPins`. Wherever a build ran after the migration, the digest was replaced by a tag.
4. **Matrix images are skipped** (`if (!isMatrix) builtImages << image` in DockerImages' Jenkinsfile). Keycloak is a matrix image with a named, mutable tag (`26.7.3-postgres-health-ispn`), so its migrated digest was never replaced, even though `keycloak/deploy-pins.json` lists both KeycloakDeploy stages. On 2026-09-25 it was the only digest pin in any live workload.
5. **registry-cleanup still assumes HelmCharts re-resolves at deploy time.** Its GC runs `--delete-untagged`. DockerImages #2548 rebuilt keycloak onto the same tag (09-24 19:12Z), the pinned digest `fa01e691` went untagged, and the GC deleted it (09-25 01:32Z). The pod kept running from the node's cache until the power cut forced a pull, which failed. Keycloak down then took DHCP down.

###### To reconsider

- **Tags everywhere, digests nowhere?** This only holds if tags are immutable. A matrix image rebuilt onto `26.7.3-postgres-health-ispn` changes what a tag pin runs on the next pull, because the pods use `imagePullPolicy: Always`.
- **What a matrix image's pin should be.** For example, a per-build tag next to the named one (`26.7.3-postgres-health-ispn-<build>`), written by the pin stage like every other image's.
- **The migrated comment.** The comment the migration left in every deploy repo ("each image at the digest the release runs") is wrong wherever a tag is pinned.
- **D53's text** in AnsibleSpecs `argo-cd/decisions.md`. The cleanup side is DI-5 and DI-8.

###### The paused cleanup job

The 2026-09-25 stop-gap pauses registry-cleanup with `suspend: true` in RegistryDeploy `chart/templates/registry-cleanup-cronjob.yaml`, so no image is deleted until this is settled. **The pause must be lifted again.** That is DI-8's to do, once cleanup can no longer delete a pinned image. This card decides half of that: with immutable per-build tags and no digest pins, `--delete-untagged` can no longer orphan a pin, but the keep-newest cap still has to respect deploy pins (DI-8). Don't close this card or DI-8 with the CronJob still suspended and no ruling on who lifts the pause.

###### Current state

KeycloakDeploy pins `@sha256:45ae4959…` for both stages (075184c, the 2026-09-25 fix), which is the current digest of `26.7.3-postgres-health-ispn`. Its `rebuild-at` is 2026-10-01. The next keycloak rebuild orphans that digest the same way, and only the paused cleanup keeps it from being deleted.

##### Comments

None.
